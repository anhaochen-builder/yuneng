# 驭能 — 新能源场站非计划停机智能诊断系统

> 产品需求与功能说明文档 v3.0  
> 编写日期：2026-08-04  
> 项目地址：`/home/an/项目/驭能/`  
> 访问地址：`http://localhost:8080`

---

## 一、产品概述

### 1.1 产品定位

**驭能**是一款面向新能源场站（风电/光伏/储能）的非计划停机智能诊断系统。当风机、逆变器、变压器等核心设备突发故障时，系统在 60 秒内自动完成**故障诊断 → 根因分析 → 处置方案 → 决策指导**的全链路闭环。

一句话定义：**给新能源场站配一个"永不休息的急诊专家"**。

### 1.2 目标用户

| 角色 | 核心诉求 |
|------|---------|
| 场站运维人员 | 告警来了能自动诊断，不用翻手册、打电话给厂家 |
| 运维主管/站长 | 知道诊断靠不靠谱（置信度），哪些设备需要重点关注 |
| 集团公司/调度 | 多场站统一监控，数据不出场，满足安全合规 |
| 第三方运维公司 | 私有化部署，服务多个客户场站 |

### 1.3 核心业务场景

**场景一：逆变器通讯中断**
> 3 号逆变器通讯中断，SCADA 报 ALM-001。运维人员可上传红外热像图。系统 60s 内给出诊断报告：IGBT 过热导致保护停机 → 散热风扇检修 → 安规操作步骤。

**场景二：风机振动超标停机**
> 1 号风机振动超标紧急停机。系统自动关联风速数据(28.5m/s)，诊断为瞬时大风导致叶片失衡，非机械故障，避免错误拆解检修。

**场景三：批量设备监控**
> 24h 监控全场站设备状态，发现温度/振动偏离正常范围自动告警，运维人员在监控大屏一目了然。

---

## 二、系统架构

### 2.1 后端架构

```
┌─────────────────────────────────────────────────┐
│                    API 层                         │
│  /api/chat  /api/diagnose  /api/alarm             │
│  /api/knowledge  /api/scada  /api/feedback        │
│  /api/trace  /api/dashboard  /api/audit           │
│  统一响应格式: {code, data, message}              │
├─────────────────────────────────────────────────┤
│              LangGraph 编排层 ★核心★               │
│  Supervisor + 8 子Agent + 12 Hook + 35 状态键     │
│  precheck → router → [并行] → diagnosis(9节点)    │
│  → judge(5维度) → report → memory_save            │
├─────────────────────────────────────────────────┤
│          AI 层 (DeepSeek V4 + R1 Ensemble)        │
│  三模部署: 在线(DeepSeek) / 离线(Ollama) / 纯离线(规则)  │
├─────────────────────────────────────────────────┤
│    知识层 (160条知识 + 48设备图谱 + 三重检索)       │
│    SCADA层 (Modbus/IEC61850/OPC UA + 环形缓冲)     │
│    多模态 (Qwen-VL-Max + EasyOCR + Cross-Attention)│
│    记忆层 (短期/工作/长期 + 时间衰减)              │
│    学习层 (反馈→案例→Skill→LoRA)                  │
│    MCP工具 (6个标准化工具)                         │
└─────────────────────────────────────────────────┘
```

### 2.2 前端架构

```
Vue 3 + TypeScript + Element Plus + ECharts + Three.js
├── 总览看板 Dashboard        — 诊断统计 + 设备健康 + 告警分布
├── 智能诊断中心 Diagnostic    — SSE流式对话 + 完整报告
├── SCADA 数据看板             — 4图表 + 设备管理 + 环境面板
├── 告警管理 Alarms            — 告警接收 + 自动诊断
├── 设备监控 Monitor           — 24h实时轮询 + 筛选排序
├── 报表管理 Reports           — 诊断报告查看 + CSV/JSON导出
├── 知识库 Knowledge           — 160条知识检索 + 文档上传
├── 设备管理 Devices           — 设备列表 + 详情 + 添加
├── 系统设置 Settings          — 系统信息 + 反馈学习 + 技能管理
└── 诊断回放 Trace             — 全链路轨迹时间线
```

---

## 三、功能模块详解

### 3.1 智能诊断（核心）

**入口**：底部导航「智能诊断」

| 功能点 | 描述 |
|--------|------|
| 文本诊断 | 输入故障描述（如"3号逆变器通讯中断"），AI 多智能体协同诊断 |
| 多模态诊断 | 上传红外热像图/设备照片/现场录音，系统自动分析并融合到诊断 |
| SSE 流式输出 | 实时推送诊断进度（开始→分析→诊断→完成），打字机效果 |
| 完整诊断报告 | **告警摘要** + **可能原因（按概率排序）** + **详细分析** + **处置建议** + **操作步骤（含安全注意/工具/耗时）** + **安全审查** |
| 置信度评分 | Judge Agent 五维度评分（证据/逻辑/安规/操作/历史），0-100 分 |
| 风险等级 | CRITICAL / HIGH / MEDIUM / LOW，不同颜色标记 |
| 闭环重规划 | 置信度 < 70 自动触发重诊断（最多 2 次），仍不达标降级人工介入 |
| 用户反馈 | 准确/部分准确/不准确 → 驱动学习系统 |

### 3.2 多模态融合诊断

| 模态 | 能力 |
|------|------|
| 文本 | DeepSeek V4 Pro 语义理解、实体提取、意图识别 |
| 图像 | Qwen-VL-Max 分析红外热像（温度异常检测）/ 设备外观（损伤/腐蚀）/ 频谱图 |
| 音频 | AST 模型识别异常声音（轴承摩擦/齿轮断齿/电弧放电/变压器嗡声） |
| OCR | EasyOCR 1.7.2 扫描件文字识别（三引擎降级：PaddleOCR→EasyOCR→pytesseract） |
| Cross-Attention | 文本(Query) × 图像/音频(Key/Value) 跨模态注意力融合 |

### 3.3 设备实时监控

**入口**：底部导航「设备监控」

| 功能点 | 描述 |
|--------|------|
| 设备状态网格 | 12 台模拟设备 × 6 种类型（风机/逆变器/变压器/储能/SVG/保护） |
| 实时指标 | 温度 / 振动 / 功率 / 转速，颜色标记异常值 |
| 自动轮询 | 可调节间隔（默认 5s），开关控制 |
| 状态筛选 | 全部 / 正常 / 告警 / 故障 / 离线 + 设备类型 + 排序 |
| 详情弹窗 | 点击卡片查看设备完整信息 + 一键触发 AI 诊断 |
| 故障脉冲动画 | 故障设备红色呼吸灯 + 红色边框高亮 |

### 3.4 告警管理

| 功能点 | 描述 |
|--------|------|
| 告警接收 | 填写告警编号/设备ID/类型/级别/描述/阈值，提交到系统 |
| 自动诊断 | 接收告警后自动触发 AI 诊断（auto_diagnose=True） |
| 告警记录 | 列表展示历史告警，区分已诊断/已接收状态 |
| 告警浮层 | 右上角实时告警通知滑入，critical 级别带红色动画 |

### 3.5 诊断报表管理

| 功能点 | 描述 |
|--------|------|
| 报告列表 | 表格展示所有诊断任务（设备/故障/风险/置信度/时间） |
| 搜索筛选 | 关键词 + 风险等级 + 设备类型 + 日期范围 |
| 详情弹窗 | 查看完整诊断报告（故障描述 + 根因分析） |
| CSV 导出 | 带表头的 UTF-8 CSV 文件 |
| JSON 导出 | 格式化 JSON 文件 |

### 3.6 知识库管理

| 功能点 | 描述 |
|--------|------|
| 知识库规模 | 160 条知识文档（86 条通用 + 74 条 Fuhrlander 实测） |
| 知识图谱 | 48 种设备 × 4 维度（告警/原因/排查/安全） |
| 真实数据 | Fuhrlander 5 年运行数据（369 告警 + 314 SCADA 变量 + 10202 事件） |
| 智能检索 | 向量检索 + BM25 关键词 + RRF 融合 + BGE Reranker 精排 |
| 文档上传 | 支持 PDF/DOCX/XLSX/TXT/MD 多格式解析 |

### 3.7 设备管理

| 功能点 | 描述 |
|--------|------|
| 设备列表 | 8 台设备（风机/逆变器/变压器/汇流箱/SVG） |
| 统计卡片 | 运行中 / 告警 / 故障 / 总计 |
| 搜索过滤 | 按设备ID/名称/类型/位置搜索 |
| 详情面板 | 点击设备查看完整属性 + 一键跳转诊断 |
| 添加设备 | 弹窗表单，支持 7 种设备类型，调用 SCADA API 注册 |

### 3.8 系统设置

| 功能点 | 描述 |
|--------|------|
| 系统信息 | 项目名称/版本/框架/模型/数据库等完整信息 |
| 服务状态 | 后端/DeepSeek/知识库/OCR/Neo4j/Docker 实时状态 |
| 资源统计 | 子智能体/MCP工具/Hook/API端点 使用率 |
| 部署模式 | 纯离线 / 标准离线 / 生产在线 三种模式说明 |
| 反馈学习 | 准确/部分准确/不准确 统计 + 案例入库/Skill生成/LoRA进度 |
| 技能管理 | Skills 列表 + SubAgents 状态 + 映射验证 |

### 3.9 诊断过程回放

| 功能点 | 描述 |
|--------|------|
| 时间线视图 | 按诊断流程展示每个节点的输入/输出 |
| 任务查询 | 输入 Task ID 搜索历史诊断轨迹 |

### 3.10 SCADA 数据看板

| 功能点 | 描述 |
|--------|------|
| 4 个 ECharts 图表 | 24h功率/发电趋势/SCADA双轴/设备健康雷达 |
| 设备管理 | 连接设备列表 + 协议/状态 |
| 环境面板 | 温度/湿度/辐照度/风速 |
| 连接表单 | 配置设备ID/类型/协议/IP/端口 |

---

## 四、技术特性

### 4.1 多智能体编排

| 子Agent | 功能 | 内部节点 |
|---------|------|---------|
| Diagnosis | 故障诊断核心引擎 | 9 节点（实体提取→知识检索→计划制定→并行执行→证据验证→诊断→重规划→风险评估→处置建议） |
| SCADA | 数据采集与分析 | 4 节点 |
| Multimodal | 多模态融合诊断 | 3 节点（图像分析→音频分析→Cross-Attention融合） |
| KnowledgeQA | 知识库问答 | 6 节点（改写→检索→重排→回答→审核→引用） |
| Judge | 诊断质量评审 | 1 节点（5维度评分） |
| Report | 诊断报告生成 | 2 节点 |
| Chat | 通用对话 | 1 节点 |
| Predictive | 时序预测监控 | 3 节点（趋势→越限→预警） |

### 4.2 12 个生命周期 Hook

| Hook | 执行时机 | 功能 |
|------|---------|------|
| PreRouteHook | 请求进入 | 输入清洗、XSS 过滤 |
| SafetyCheckHook | 请求进入 | 敏感词拦截 |
| PostRouteHook | 路由后 | 意图校验、非法降级 |
| PreRagHook | RAG 前 | Query 改写、设备类型补全 |
| PostRagHook | RAG 后 | 召回质量检查 |
| PreToolUseHook | 工具调用前 | 权限校验、风险标记 |
| PostToolUseHook | 工具调用后 | 数据脱敏（手机/邮箱） |
| PreDiagnosisHook | 诊断前 | 证据充足性检查 |
| PostDiagnosisHook | 诊断后 | 风险提示补全 |
| HumanApprovalHook | 诊断后 | 高风险审批创建 |
| AuditHook | 诊断后 | 审计日志 |
| DataMaskingHook | 输出前 | 二次脱敏 |

### 4.3 三种部署模式

| 模式 | LLM | 数据库 | 适用场景 |
|------|-----|--------|---------|
| 纯离线 | 规则引擎 | ChromaDB(SQLite) | 涉密场站/无互联网 |
| 标准离线 | 本地 Ollama | ChromaDB | 有 GPU 的场站 |
| 生产在线 | DeepSeek API | ChromaDB + PostgreSQL(可选) | 全场站，性能最优 |

### 4.4 学习闭环

```
诊断完成 → 用户反馈"准确" → 案例向量化入库(ChromaDB)
                         → 同模式≥3次 → Skill自动生成
                         → 累积≥50例 → LoRA增量微调
用户反馈"不准" → 记录为负样本 → 待审核池
```

---

## 五、API 接口总览

### 5.1 核心接口

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/health` | 健康检查 |
| POST | `/api/chat` | 同步对话 |
| POST | `/api/chat/stream` | SSE 流式对话 |
| POST | `/api/diagnose` | 同步诊断 |
| POST | `/api/diagnose/stream` | SSE 流式诊断 |
| POST | `/api/diagnose/multimodal` | 多模态诊断 |
| POST | `/api/diagnose/multimodal/stream` | SSE 流式多模态诊断 |
| POST | `/api/alarm/receive` | 告警接收（含自动诊断） |
| POST | `/api/alarm/diagnose` | 告警诊断 |
| GET | `/api/knowledge/search/test` | 知识库检索 |
| POST | `/api/knowledge/documents/upload` | 文档上传 |
| POST | `/api/scada/connect` | SCADA 设备连接 |
| GET | `/api/scada/data/{id}` | 设备数据查询 |
| POST | `/api/feedback` | 诊断反馈 |
| GET | `/api/trace/{id}/replay` | 诊断轨迹回放 |
| GET | `/api/dashboard` | 项目进度面板 |
| GET | `/api/audit` | 质量审计报告 |

### 5.2 统一响应格式

```json
{
  "code": 0,
  "data": { ... },
  "message": "success"
}
```

---

## 六、部署说明

### 6.1 环境要求

| 依赖 | 版本 | 说明 |
|------|------|------|
| Python | 3.11+ | 后端运行时 |
| Node.js | 18.20+ | 前端构建 |
| 磁盘 | 10GB+ | 知识库 + 模型缓存 |
| 内存 | 4GB+ | 双模型需 8GB |

### 6.2 快速启动

```bash
# 后端
cd /home/an/项目/驭能
source venv/bin/activate
bash start.sh

# 前端开发
cd frontend && npm run dev

# 前端构建（生产）
cd frontend && npm run build
```

### 6.3 访问地址

```
http://localhost:8080   # 生产模式（前后端一体）
http://localhost:5173   # 开发模式（仅前端，Vite HMR）
```

---

## 七、数据统计

| 指标 | 数值 |
|------|------|
| 子智能体 | 8 个 |
| 生命周期 Hook | 12 个 |
| MCP 工具 | 6 个 |
| API 端点 | 30+ |
| 知识库条目 | 160 条 |
| 设备类型覆盖 | 48 种（10 大类别） |
| 真实告警类型 | 369 种（Fuhrlander） |
| SCADA 变量 | 314 个 |
| 前端页面 | 9 个 |
| 前端组件 | 14 个 |
| 后端源文件 | 50+ |
| 代码行数 | 15000+ |
