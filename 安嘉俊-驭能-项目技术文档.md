# 驭能 — 新能源场站非计划停机智能诊断系统

> **项目技术文档 v3.0**
>
> 文档状态：2026-07-29 | 部署地址：内网 / Docker
> 目标市场：宁夏电力市场新能源场站

---

## 目录

1. [项目概述](#一项目概述)
2. [系统架构总览](#二系统架构总览)
3. [LangGraph 多智能体编排层](#三langgraph-多智能体编排层)
4. [核心功能模块](#四核心功能模块)
5. [知识库与 RAG 检索层](#五知识库与-rag-检索层)
6. [三层记忆系统](#六三层记忆系统)
7. [MCP 工具层](#七mcp-工具层)
8. [API 接口总览](#八api-接口总览)
9. [完整数据流](#九完整数据流)
10. [部署架构](#十部署架构)
11. [产品核心优势总结](#十一产品核心优势总结)
12. [总结](#十二一句话总结)

---

## 一、项目概述

### 1.1 产品定位

驭能是一个面向新能源场站的**智能急诊系统**。当风机、光伏逆变器等核心设备突发故障时，系统在 3-5 分钟内自动完成**故障诊断 → 处置方案 → 决策指导**的全链路闭环。

> **一句话定义**：给新能源场站配一个"永不休息的急诊专家"。

### 1.2 业务场景

#### 场景一：逆变器通讯中断
某光伏电站的 3 号逆变器突然通讯中断，SCADA 后台弹出 ALM-001 告警。传统模式下，运维人员需要赶赴现场、查阅手册、联系厂家，整个过程耗时 2-4 小时。驭能在收到告警后自动抓取故障前后 5 分钟的 SCADA 时序数据（温度、功率、电压、电流），同时检索历史相似案例和安规条款，3 分钟内输出完整的诊断报告和处置方案。

#### 场景二：风机振动超标停机
某风电场 1 号风机触发"振动超标"告警后紧急停机。驭能自动采集故障窗口数据，结合气象数据（风速 28.5m/s）和振动频谱特征，识别出根因为瞬时大风导致叶片受力失衡，而非齿轮箱机械故障，避免了错误的拆解检修。

#### 场景三：批量巡检辅助
运维人员在例行巡检中发现多台设备参数异常，通过对话界面逐一提问，系统聚合诊断结果，生成巡检报告，标注风险等级和优先级。

### 1.3 核心价值

| 维度 | 当前痛点 | 驭能价值 |
|------|---------|-------------|
| **时效** | 非计划停机后人工排查耗时数小时 | 多智能体协同诊断 < 60 秒 |
| **质量** | 运维人员经验参差不齐，根因误判率高 | DeepSeek V4 Pro + 多源证据交叉验证 |
| **安全** | 处置方案缺乏安规合规审查 | 自动审查 + 安规条款精准引用 |
| **数据** | 诊断依赖人工描述，缺乏实时数据支撑 | SCADA数据连接器自动抓取故障前后时序数据 |
| **进化** | 系统静态，无法从诊断中学习 | 主动学习 + 成功案例自动沉淀 |

### 1.4 技术栈总览

| 层级 | 技术 | 版本 | 选型理由 |
|------|------|------|---------|
| 后端框架 | FastAPI + Uvicorn | 0.115+ | 异步高性能，原生 OpenAPI 支持，生态成熟 |
| 多智能体编排 | LangGraph + Supervisor Agent | 0.2+ | 状态驱动图编排，支持分支/循环/条件路由 |
| LLM | DeepSeek V4 Pro | deepseek-chat | 中文推理能力行业领先，成本仅为 GPT-4o 的 1/10 |
| 向量数据库 | ChromaDB | 0.5+ | 轻量嵌入式，支持 HNSW 索引，零运维成本 |
| 嵌入模型 | BAAI/bge-large-zh-v1.5 | — | C-MTEB 中文榜单 Top3，1024 维，专为中文优化 |
| 工具协议 | MCP (Model Context Protocol) | 1.5+ | Anthropic 标准协议，跨平台工具互操作 |
| 工业协议 | Modbus TCP / IEC 61850 / OPC UA | — | 电力行业三大标准工业协议 |
| 多模态模型 | Qwen-VL-Max / InternVL2 | — | 阿里/商汤旗舰多模态模型，中文场景最优 |
| 音频分析 | Audio Spectrogram Transformer (AST) | — | 谷歌研究院模型，声学事件分类 SOTA |
| OCR | PaddleOCR | — | 百度开源，中文准确率 > 95%，支持表格结构还原 |
| 前端 | Vue 3 + Element Plus | 3.5+ | 渐进式框架，组件丰富，学习成本低 |
| 容器化 | Docker Compose | 3.8 | 一键部署，环境一致性 |

### 1.5 三种部署模式

系统支持三种部署模式，按场景灵活切换：

| 模式 | 数据库 | LLM | 适用场景 |
|------|--------|-----|---------|
| **纯离线** | ChromaDB (SQLite) | 降级到规则引擎 | 涉密场站 / 无互联网环境 |
| **标准离线** | ChromaDB | 本地 Ollama | 有 GPU 的场站，可运行本地大模型 |
| **生产在线** | PostgreSQL + ChromaDB | DeepSeek API | 全场站使用，性能最优 |

---

## 二、系统架构总览

### 2.1 六层架构

系统自下而上分为**六层**，各层职责分明、松耦合：

| 层序 | 层次名称 | 职责 | 核心技术 |
|------|---------|------|---------|
| 第一层 | **数据采集层** | 从物理世界获取原始数据 | Modbus TCP / IEC 61850 / OPC UA，Qwen-VL，AST |
| 第二层 | **知识层** | 结构化存储和检索领域知识 | ChromaDB，Neo4j(可选)，PyMuPDF，PaddleOCR |
| 第三层 | **MCP 工具层** | 提供标准化工具接口 | MCP Protocol 1.5+，6 个核心工具 |
| 第四层 ★ | **编排层** | 多智能体协作调度，全流程编排 | LangGraph，Supervisor Pattern，StateGraph |
| 第五层 | **记忆与学习层** | 跨会话知识复用和自我进化 | 三层记忆，主动学习引擎，LoRA 微调 |
| 第六层 | **API 与前端层** | 对外 API 和用户界面 | FastAPI，SSE 流式，Vue 3 SPA |

### 2.2 层次间数据流转

数据自底向上流动，控制流自顶向下传递：

- **上行数据流（采集 → 知识 → 编排）**：SCADA 连接器从物理设备采集实时数据 → 经过协议解析和标准化 → 注入知识层的向量检索和知识图谱 → 最终作为编排层诊断 Agent 的输入
- **下行控制流（编排 → 工具 → 采集）**：Supervisor Agent 做出路由决策 → 通过 MCP 协议调用工具 → 工具访问数据采集层获取最新数据
- **横向记忆流（编排 ↔ 记忆）**：每次诊断完成后 → 结果写入三层记忆 → 下次诊断时从记忆层加载上下文

### 2.3 架构全景图

    ┌──────────────────────────────────────────────────────────────────┐
    │                        第六层：API与前端层                          │
    │                                                                   │
    │  FastAPI REST API          │  SSE 流式输出       │  Vue 3 SPA     │
    │  /api/chat  /api/diagnose │  实时推送诊断进度    │  诊断对话界面   │
    │  /api/alarm  /api/knowledge│  逐字流式输出        │  历史记录管理   │
    │  /api/trace  /api/feedback │                     │  知识库管理     │
    │  /api/scada  /api/tools    │                     │  系统设置       │
    └──────────────────────────────┬───────────────────────────────────┘
                               │
    ┌──────────────────────────────▼───────────────────────────────────┐
    │                      第五层：记忆与学习层                           │
    │                                                                   │
    │  三层记忆                         主动学习闭环                     │
    │  ├─ 短期记忆 (当前会话, 最近3轮)    ├─ 用户反馈"准确" → 案例入库   │
    │  ├─ 工作记忆 (任务状态, 35状态键)   ├─ 故障模式≥3次 → Skill自动生成│
    │  └─ 长期记忆 (ChromaDB, 永久)      └─ 案例≥50个 → LoRA增量微调    │
    └──────────────────────────────┬───────────────────────────────────┘
                               │
    ┌──────────────────────────────▼───────────────────────────────────┐
    │                 第四层：LangGraph 多智能体编排层 ★核心★              │
    │                                                                   │
    │  Supervisor Pattern: 1个总管智能体 + 7个专业智能体                 │
    │                                                                   │
    │  START → PreCheck → ContextLoad → Supervisor → [条件路由分发]       │
    │              ┌──────────────────┬──────────────────┐              │
    │              ▼                  ▼                  ▼              │
    │       Diagnosis子图        SCADA采集子图       多模态子图          │
    │       (9节点+闭环           (数据连接器)        (文本+图像+声音)   │
    │        重规划机制)                                                │
    │              └──────────────────┴──────────────────┘              │
    │                               │                                   │
    │                               ▼                                   │
    │                      Judge 评估子图                                │
    │                    (5 维度量化评分)                                │
    │                               │                                   │
    │                   ┌───────────┴───────────┐                       │
    │              ＜ 0.7?               ≥ 0.7?                        │
    │                │                       │                          │
    │          重规划(最多2次)         SafetyReview                      │
    │          仍不达标→降级人工           │                             │
    │                                     ▼                             │
    │                             FinalResponse → MemorySave → END      │
    │                                                                   │
    │  35 个状态键在节点间流转  │  12 个 Hook 生命周期拦截器             │
    └──────────────────────────────┬───────────────────────────────────┘
                               │
    ┌──────────────────────────────▼───────────────────────────────────┐
    │                        第三层：MCP 工具层                          │
    │                                                                   │
    │  ┌─────────────────┐ ┌──────────────────┐ ┌──────────────────┐   │
    │  │ get_device_     │ │ get_alarm_       │ │ get_device_      │   │
    │  │ status          │ │ history          │ │ logs             │   │
    │  └─────────────────┘ └──────────────────┘ └──────────────────┘   │
    │  ┌─────────────────┐ ┌──────────────────┐ ┌──────────────────┐   │
    │  │ get_defect_     │ │ search_safety_   │ │ get_device_      │   │
    │  │ tickets         │ │ rules            │ │ profile          │   │
    │  └─────────────────┘ └──────────────────┘ └──────────────────┘   │
    │                                                                   │
    │  标准 MCP 协议 (Anthropic)，可接入任何 MCP 兼容工具市场             │
    └──────────────────────────────┬───────────────────────────────────┘
                               │
    ┌──────────────────────────────▼───────────────────────────────────┐
    │                       第二层：知识层                                │
    │                                                                   │
    │  ┌──────────────────────────┐  ┌─────────────────────────────┐   │
    │  │  RAG 知识检索             │  │  知识图谱                    │   │
    │  │  ├─ 向量检索 (BGE 1024维) │  │  ├─ 10大类别 × 48种设备    │   │
    │  │  ├─ BM25 关键词检索       │  │  ├─ 100+ 故障模式           │   │
    │  │  ├─ RRF 融合算法          │  │  ├─ 30+ 操作实体            │   │
    │  │  └─ BGE Reranker 精排    │  │  └─ 20+ 安全实体            │   │
    │  └──────────────────────────┘  └─────────────────────────────┘   │
    │  ┌──────────────────────────────────────────────────────────┐    │
    │  │  文档解析引擎                                              │    │
    │  │  ├─ PDF (文本版/扫描版)    ├─ Word (.docx)                │    │
    │  │  ├─ Excel (.xlsx)          ├─ TXT / Markdown              │    │
    │  │  ├─ PaddleOCR 扫描件识别   ├─ TableBank 表格提取          │    │
    │  │  └─ LayoutParser 版面分析  └─ 章节层级重建                │    │
    │  └──────────────────────────────────────────────────────────┘    │
    └──────────────────────────────┬───────────────────────────────────┘
                               │
    ┌──────────────────────────────▼───────────────────────────────────┐
    │                      第一层：数据采集层                             │
    │                                                                   │
    │  ┌──────────────────┐ ┌──────────────────┐ ┌──────────────────┐   │
    │  │ SCADA 连接器      │ │ 多模态采集器      │ │ 公开数据集        │   │
    │  │ ├─ Modbus TCP    │ │ ├─ 红外热像       │ │ ├─ 龙源风电       │   │
    │  │ ├─ IEC 61850     │ │ ├─ 现场照片       │ │ ├─ 山西泽州       │   │
    │  │ └─ OPC UA        │ │ └─ 告警录音       │ │ └─ 国网多模态     │   │
    │  └──────────────────┘ └──────────────────┘ └──────────────────┘   │
    │  ┌──────────────────────────────────────────────────────────┐    │
    │  │  环形缓冲区 (RingBuffer)                                    │    │
    │  │  ├─ 滚动存储 30 分钟时序数据                                │    │
    │  │  └─ 告警触发 → 自动提取 ±5 分钟故障窗口                     │    │
    │  └──────────────────────────────────────────────────────────┘    │
    └──────────────────────────────────────────────────────────────────┘

---

## 三、LangGraph 多智能体编排层

### 3.1 为什么选择 LangGraph

LangGraph 是 LangChain 生态中专门用于构建**有状态的多智能体系统**的图形化框架。其核心设计理念是：多智能体工作流本质上是**图结构**，而非线性链。

传统 LLM 应用采用"提示 → LLM → 响应"的线性模式，难以应对真实智能体系统的复杂性。生产环境中的多智能体协作需要六大核心能力：

| 能力 | 说明 | LangGraph 支持方式 |
|------|------|-------------------|
| **分支** | 基于数据选择不同执行路径 | add_conditional_edges — 定义条件边的路由函数 |
| **循环** | 支持重试与迭代优化 | 节点闭环 — 节点可以指向自身或上游节点，形成重试链路 |
| **汇合** | 多个智能体向共享状态写入数据 | StateGraph — 所有节点共享同一个状态字典 |
| **条件路由** | 根据执行结果动态决定后续流程 | 自定义路由函数 — 读取状态值，返回下一节点名称 |
| **可中断** | 人工审批节点，支持暂停/恢复 | interrupt_before / interrupt_after — 在指定节点前后挂起 |
| **可观测** | 完整执行轨迹和状态快照 | LangSmith + Checkpointer — 记录每一步的输入输出和状态变化 |

LangGraph 通过**状态驱动、可视化、可中断**三大特性，防止智能体"抽风"，成为构建企业级可靠 Agent 的首选框架。在学术研究和工业实践中，基于 LangGraph 的多智能体系统在任务完成率和响应质量上显著优于传统的线性链式 Agent。

### 3.2 Supervisor Pattern（总管模式）

系统采用 LangGraph Supervisor Pattern 作为多智能体协作的核心架构。这是当前业界公认的多智能体编排最佳实践——一个总管智能体负责全局调度，多个专业智能体各司其职。

**架构示意**：

                    ┌──────────────────────┐
                    │   Supervisor Agent    │  总管智能体
                    │                      │
                    │  • 意图识别（9分类）   │
                    │  • 任务分解            │
                    │  • 智能路由            │
                    │  • 结果聚合            │
                    │  • 状态管理            │
                    │  • 质量监控            │
                    └──┬───┬───┬───┬───┬──┘
                       │   │   │   │   │
          ┌────────────┘   │   │   │   └────────────┐
          ▼                ▼   ▼   ▼                ▼
    ┌──────────┐  ┌──────────┐  ...  ┌──────────┐  ┌──────────┐
    │Diagnosis │  │  SCADA   │       │MultiModal│  │  Judge   │
    │  Agent   │  │  Agent   │       │  Agent   │  │  Agent   │
    └──────────┘  └──────────┘       └──────────┘  └──────────┘

**Supervisor Agent 的六大职责**：

| 职责 | 说明 | 实现方式 |
|------|------|---------|
| **意图识别** | 接收用户输入，判断用户想做什么 | RouterAgent 调用 DeepSeek V4 Pro，进行 9 分类意图分类 |
| **任务分解** | 将复杂任务拆解为可并行/串行执行的子任务 | LLM 根据 Prompt 模板生成任务分解计划 |
| **智能路由** | 根据意图类型将任务分发给对应的专业智能体 | IntentDispatcher 条件路由，映射 9 种意图到 3 条子图 |
| **结果聚合** | 收集各子智能体的执行结果，融合为统一上下文 | 将并行子 Agent 的结果文本化后合并 |
| **状态管理** | 维护全局 35 个状态键，控制任务生命周期 | StateGraph 状态字典在节点间自动传递 |
| **质量监控** | 监控置信度，低于阈值自动触发重规划 | Judge Agent 评分 → 置信度 < 0.7 → 重规划循环 |

**路由决策表**：

| 用户意图 | 英文标识 | 路由目标 |
|---------|---------|---------|
| 故障诊断（如"#3逆变器通讯中断"） | FAULT_DIAGNOSIS | Diagnosis 子图 |
| 告警诊断分析 | ALARM_DIAGNOSIS / ALARM_ANALYSIS | Diagnosis 子图 |
| 日志分析 | LOG_ANALYSIS | Diagnosis 子图 |
| 工单查询 | TICKET_QUERY | Diagnosis 子图 |
| 知识问答（如"逆变器散热不良怎么处理"） | KNOWLEDGE_QA | KnowledgeQA 子图 |
| 安全规程问答（如"停电操作流程是什么"） | SAFETY_QA | KnowledgeQA 子图 |
| 设备状态查询 | DEVICE_STATUS | KnowledgeQA 子图 |
| 设备台账查询 | DEVICE_PROFILE | KnowledgeQA 子图 |
| 告警历史查询 | ALARM_QUERY | KnowledgeQA 子图 |
| 通用对话（如"你好"） | GENERAL_CHAT | Chat 子图 |

### 3.3 状态管理

系统定义 **35 个状态键**，在 Graph 中统一流转。根据更新策略分为 Replace（覆盖）和 Append（追加）两种：

| 分类 | 状态键 | 策略 | 生命周期 | 说明 |
|------|--------|------|---------|------|
| **输入层** | input | Replace | 单次请求 | 用户原始输入 |
| | cleaned_input | Replace | 单次请求 | 清洗后的输入（去XSS、截断等） |
| | rewritten_query | Replace | 单次请求 | LLM 改写后的查询关键词 |
| **会话层** | session_id | Replace | 会话期间 | 对话会话标识 |
| | task_id | Replace | 单次任务 | 诊断任务唯一标识 |
| | user_id | Replace | 会话期间 | 操作用户身份标识 |
| | trace_id | Replace | 单次请求 | 链路追踪标识 |
| **意图层** | intent | Replace | 单次请求 | 路由意图（9种之一） |
| | confidence | Replace | 单次请求 | 意图分类置信度 |
| | entities | Replace | 单次请求 | 提取的实体（设备类型/ID/故障关键词） |
| **诊断层** | diagnosis_result | Replace | 单次任务 | 诊断结果（根因列表+分析报告） |
| | risk_level | Replace | 单次任务 | 风险等级（CRITICAL/HIGH/MEDIUM/LOW） |
| | next_action | Replace | 单次任务 | 下一步行动指令 |
| **证据层** | evidence | Replace | 单次任务 | 证据链汇总 |
| | evidence_score | Replace | 单次任务 | 证据充足性评分 |
| | evidence_coverage | Replace | 单次任务 | 证据覆盖度（0-1） |
| | evidence_warnings | Replace | 单次任务 | 证据不足警告 |
| **RAG层** | rag_results | Replace | 单次任务 | RAG 检索结果文本 |
| | tool_result | Replace | 单次任务 | 工具调用结果 |
| **记忆层** | memory_context | Replace | 会话期间 | 短期记忆上下文 |
| | skill_context | Replace | 单次任务 | 匹配到的 Skill Prompt |
| | history | Replace | 会话期间 | 对话历史（最近3轮） |
| | matched_skill | Replace | 单次任务 | 匹配到的 Skill ID |
| **质量层** | judge_score | Replace | 单次任务 | Judge 评分（0-100） |
| | judge_details | Replace | 单次任务 | 评分明细 |
| | retry_count | Replace | 单次任务 | 重规划次数 |
| **SCADA层** | scada_data | Replace | 单次任务 | SCADA 时序数据 |
| | scada_timestamp | Replace | 单次任务 | 数据时间戳 |
| **多模态层** | multimodal_data | Replace | 单次任务 | 多模态原始数据 |
| | multimodal_features | Replace | 单次任务 | 多模态特征向量 |
| **执行层** | step_results | Append | 单次任务 | 各步骤执行结果列表 |
| | validation_warnings | Append | 单次任务 | 验证告警列表 |
| | plan_steps | Replace | 单次任务 | 诊断计划步骤列表 |
| | current_step_index | Replace | 单次任务 | 当前执行步骤索引 |

### 3.4 主编排图流程

完整的诊断流程从用户输入到最终输出，经过 **12 个阶段**：

**阶段一：PreCheck（输入校验）**

系统对用户输入进行安全检查。XSS 过滤器移除所有 HTML 标签和恶意脚本；长度限制器将输入截断至 5000 字符，防止超长文本占用过多 Token；敏感词拦截器检查是否包含炸弹、武器、破坏、黑客等不安全关键词。两个 Hook（PreRouteHook 和 SafetyCheckHook）在此阶段执行。

**阶段二：ContextLoad（上下文加载）**

系统加载三层上下文信息。短期记忆层提供当前会话的最近 3 轮对话历史；Skill 上下文层注入匹配到的场景化 Prompt（如新能源故障诊断的 7 步诊断流程）；工作记忆层补充当前任务的中间状态。

**阶段三：Supervisor（意图识别与路由）**

RouterAgent 调用 DeepSeek V4 Pro，将用户输入分类为 9 种意图之一。识别结果包含意图类型、置信度（0-1）、实体信息（设备类型、设备ID、故障关键词）。然后 IntentDispatcher 根据意图类型将请求路由到对应的子图。

**阶段四：并行子图执行**

根据路由结果，三个子图可能并行或串行执行。Diagnosis 子图是核心诊断流程（9 个节点），SCADA 子图负责数据采集，多模态子图负责图像和音频分析。并行执行通过异步任务调度器（asyncio.gather）实现，三个子图的结果最终汇总。

**阶段五：Judge 评估**

Judge Agent 对诊断结果进行 5 维度量化评分（0-100 分）。评分维度包括证据充分性（25%）、推理逻辑性（25%）、安规合规性（20%）、可操作性（20%）、历史一致性（10%）。

**阶段六：闭环决策**

根据 Judge 评分进行分支决策。评分 ≥ 70 分（C 级以上）直接进入安全审查环节。评分 < 70 分自动触发重规划——系统扩大检索范围、重新调用 LLM 推理、再次提交 Judge 评估。最多重试 2 次，2 次后仍不达标则降级为人工介入模式。

**阶段七：SafetyReview（安全审查）**

RiskReviewAgent 对诊断建议进行安全合规审查，检查处置方案是否符合电力安规、是否存在安全风险。高风险操作（停电、紧急派单）需标记 ⚠️ 并建议人工确认。同时执行 4 个 PostDiagnosis Hook：风险提示补全、高风险审批请求创建、审计日志记录、敏感信息脱敏。

**阶段八：FinalResponse（最终响应）**

将诊断报告（9 项结构化内容）、处置方案、安全审查结论、Judge 质量评分组装为统一输出。

**阶段九：MemorySave（记忆持久化）**

将本次对话和诊断结果持久化到三层记忆系统。短期记忆更新对话历史，长期记忆根据用户反馈决定是否将诊断结果向量化存储。

**阶段十：ActiveLearning（主动学习）**

根据用户反馈触发学习机制。用户反馈"准确"时，诊断结果向量化存入长期记忆；同一故障模式出现 3 次以上时，自动提炼为可复用 Skill；累积 50 个以上新案例时，触发 LoRA 增量微调。

### 3.5 Diagnosis 子图（9 节点 + 闭环重规划）

Diagnosis 子图是系统的大脑，包含 9 个诊断节点和完整的闭环重规划机制：

| 序号 | 节点名称 | 功能描述 | 输入数据 | 输出数据 |
|------|---------|---------|---------|---------|
| ① | Entity Extract | 从用户输入中提取关键实体：设备类型（风机/逆变器/变压器等）、设备编号（如 INV001）、故障关键词（通讯中断/温度过高/振动超标等） | 用户原始输入文本 + SCADA 数据 | 结构化实体字典：device_type, device_id, fault_keywords |
| ② | Alarm RAG | 基于实体信息进行三重混合检索：知识图谱扩展查找设备故障模式、向量检索查找语义相似案例、关键词检索精确匹配术语 | 实体信息 + LLM 改写后的 Query | Top-5 相似案例文本 + 知识图谱上下文（故障模式/原因/排查步骤） |
| ③ | Planner | LLM 根据已有上下文制定详细的诊断执行计划，明确每步做什么、用什么工具、预期产出什么 | 实体信息 + RAG 上下文 | JSON 格式的诊断步骤列表（step_id, type, action, description） |
| ④ | Executor | 并行执行 4 个专业子 Agent（regulation / metrics / log / ticket），每个子 Agent 独立运行 LLM 推理，完成后结果汇总 | 诊断计划 + 各工具返回的原始数据 | 4 份子 Agent 分析报告（安规/参数/日志/工单） |
| ⑤ | Evidence Validation | 验证证据是否充分：证据来源数量（至少 2 个）、证据间是否一致（无矛盾）、证据覆盖度是否完整 | 子 Agent 分析报告 + RAG 检索结果 | 证据评分（0-1）+ 覆盖度 + 不足标记 |
| ⑥ | Diagnose | DeepSeek V4 Pro 综合所有证据进行最终诊断，输出 9 项结构化诊断报告 | 全部证据汇总 + Skill 场景 Prompt | 9 项结构化诊断报告 + JSON 元数据 |
| ⑦ | Replanner | 当证据不足或置信度偏低时，分析证据缺口，生成补充检索和推理计划 | 证据评分 + 证据不足标记 | 补充诊断计划（新检索范围、新推理方向） |
| ⑧ | Risk Assessment | 评估诊断结论中的风险：风险等级（CRITICAL/HIGH/MEDIUM/LOW）、影响范围（单设备/多设备/全站）、紧急程度 | 诊断报告 + 安规条款 | 风险等级 + 影响范围 + 紧急程度 |
| ⑨ | Action Recommend | 生成可执行的处置方案：操作步骤（按顺序）、所需工具、安规引用、预计耗时 | 诊断报告 + 风险评估结果 | 操作步骤列表 + 工具清单 + 安全提示 |

**9 项结构化诊断报告内容**：

1. **告警摘要** — 故障事件的关键信息简述（什么设备、什么时间、什么告警）
2. **初步判断** — 基于告警信息的第一印象和初步推论
3. **分析依据** — 列出所有支撑诊断结论的数据和证据来源，严格限定只能引用工具返回的真实数据，严禁编造
4. **可能原因** — 按概率从高到低列出所有可能原因，每个原因给出百分比评估
5. **排查步骤** — 给出详细的排查步骤，每步说明目的和方法
6. **处理建议** — 给出具体处理建议（降负荷/停运检修/继续观察等）
7. **安全风险提示** — 列出操作过程中需要注意的安全风险，引用安规具体条款编号
8. **是否建议派单** — 明确给出是否建议派单的结论，如建议则注明紧急程度（紧急/一般）和派单类型（电气/机械/通讯）
9. **风险自复核** ★ — 对上述诊断建议进行自我审核：诊断结论是否有充分数据支撑？处理建议是否存在安全风险？是否遗漏了重要排查步骤？整体风险等级评估（CRITICAL/HIGH/MEDIUM/LOW）。这部分要求 LLM 诚实客观，发现不足时必须指出

**闭环重规划机制详解**：

诊断报告 → Judge Agent 五维度评分 → 分数判断

- 评分 ≥ 70 分 → 输出诊断报告，进入 Safety Review 阶段
- 评分 < 70 分（第 1 次）→ 触发重规划：系统分析 Judge 的评分明细，确定哪几个维度得分低，然后有针对性地扩大检索范围（增加 top_k、放宽时间范围），重新调用 DeepSeek V4 Pro 进行推理诊断
- 评分 < 70 分（第 2 次）→ 再次重规划，进一步扩大检索范围，更换 LLM 的推理策略（从"先证据后结论"改为"先假设后验证"）
- 评分仍 < 70 分（第 2 次之后）→ 降级为人工介入模式：系统输出当前已有的全部信息（证据、分析、初步结论），明确告知用户"自动诊断未达置信度阈值，建议人工判断"，同时将案例标记为"待专家标注"

### 3.6 并行子 Agent 执行

4 个子 Agent 通过异步任务调度器并行执行，诊断速度相比串行提升约 4 倍。每个子 Agent 独立运行 LLM 推理，互不阻塞：

| 子 Agent | 专业领域 | 输入数据源 | 分析任务 | 输出内容 |
|----------|---------|-----------|---------|---------|
| **regulation** | 安规合规 | search_safety_rules 工具返回的安规条款 | 检索与当前故障相关的安全操作规程，分析诊断建议的合规性 | 相关安规条款列表 + 合规性分析 + 安全操作注意事项 |
| **metrics** | 设备参数 | get_device_status 工具返回的实时数据 + SCADA 时序数据 | 分析设备实时运行参数（温度/振动/功率/电压/电流/频率），识别偏离正常范围的参数 | 异常参数列表 + 偏离程度评估 + 参数间相关性分析 |
| **log** | 日志分析 | get_device_logs 工具返回的运行日志 | 分析设备运行日志，按时间顺序重建故障事件时间线，识别关键事件的前后关联 | 故障时间线 + 关键事件列表 + 事件间时序关系 |
| **ticket** | 工单匹配 | get_defect_tickets 工具返回的缺陷工单历史 | 查询历史缺陷工单，判断当前故障是否存在历史相似案例，分析历史处理方案的可参考性 | 相似案例列表 + 历史处理方案 + 相似度评估 |

并行执行完成后，4 份子 Agent 报告汇聚为统一上下文，注入 DiagnosisAgent 进行综合诊断。这确保了诊断不会因为单一数据源的缺失或噪声而偏离方向。

### 3.7 Hook 生命周期拦截器

系统内置 **12 个 Hook**，在 8 个关键生命周期节点注入横切关注点。Hook 采用责任链模式，按注册顺序依次执行，单个 Hook 失败不影响其他 Hook：

| Hook 节点 | 注册的 Hook | 执行顺序 | 功能描述 |
|-----------|-----------|---------|---------|
| **PRE_ROUTE** | PreRouteHook | ① | 输入清洗：去除首尾空格、截断至 5000 字符、移除 HTML 标签和 XSS 攻击载荷 |
| | SafetyCheckHook | ② | 敏感词拦截：检查是否包含炸弹、武器、破坏、黑客等不安全关键词，命中则直接拒绝处理 |
| **POST_ROUTE** | PostRouteHook | ③ | 意图校验：检查 RouterAgent 返回的意图是否在 12 种合法意图列表中，非法意图降级为 GENERAL_CHAT |
| **PRE_RAG** | PreRagHook | ④ | Query 改写：如果实体中包含设备类型但查询中未出现，则自动补全设备类型前缀（如"通讯中断"补全为"逆变器 通讯中断"） |
| **POST_RAG** | PostRagHook | ⑤ | 召回质量检查：统计 RAG 返回结果数量，< 3 条标记为 low 质量，触发警告日志 |
| **PRE_TOOL_USE** | PreToolUseHook | ⑥ | 工具权限校验：检查调用者是否有权限使用该工具，高风险工具（get_device_status 等）额外标记 |
| **POST_TOOL_USE** | PostToolUseHook | ⑦ | 结果脱敏：对工具返回结果中的手机号（1开头11位数字）和邮箱地址进行脱敏处理 |
| **PRE_DIAGNOSIS** | PreDiagnosisHook | ⑧ | 证据充足性检查：统计证据来源数量，< 2 条标记为 evidence_insufficient，可能影响诊断置信度 |
| **POST_DIAGNOSIS** | PostDiagnosisHook | ⑨ | 风险提示补全：如果风险等级为 CRITICAL 或 HIGH，在输出末尾追加人工复核提示 |
| | HumanApprovalHook | ⑩ | 审批请求创建：高风险诊断结论自动创建审批请求，标记 approval_required |
| | AuditHook | ⑪ | 审计日志记录：记录用户 ID、意图类型、置信度、风险等级等关键信息 |
| | DataMaskingHook | ⑫ | 二次脱敏：对最终输出中的手机号、密码、密钥等敏感信息进行再次检查和脱敏 |

### 3.8 知识问答子图（KnowledgeQA）

当用户意图为知识问答类时（KNOWLEDGE_QA、SAFETY_QA、DEVICE_STATUS 等），系统进入 KnowledgeQA 子图，包含 7 个节点：

| 节点 | 功能 |
|------|------|
| QueryRewrite | LLM 将用户的自然语言问题改写为更适合检索的关键词组合，保留核心语义 |
| RAG Retrieve | 执行混合检索（向量 + BM25），从知识库中召回前 10 个最相关的文档片段 |
| Rerank | BGE Reranker 对检索结果进行交叉编码精排，取前 5 个最优结果 |
| ReAct QA | LLM 基于检索到的参考资料回答用户问题，优先使用参考资料中的信息，在回答末尾列出引用来源 |
| AnswerReview | 对回答进行质量评估：长度检查（不能 < 50 字符）、事实一致性检查（不能与参考资料矛盾） |
| 质量门控 | 如果回答质量不达标（太短或事实不一致），且重试次数 < 2，则返回 RAG Retrieve 重新检索；否则接受当前回答 |
| Citation | 为回答中的关键事实添加引用标注，链接回原始文档来源 |

---

## 四、核心功能模块

### 4.1 SCADA 数据连接器

#### 4.1.1 功能概述

SCADA 数据连接器是系统与物理设备之间的桥梁。当告警触发时，系统自动抓取故障设备在**故障时刻前后各 5 分钟**的完整时序数据（电压、电流、功率、温度、振动、频率等），作为诊断的原始输入。

其核心价值在于：运维人员不再需要手动登录 SCADA 系统、查找设备、截图保存、人工描述，整个过程从原来的 1-3 分钟人工操作缩短为不到 3 秒的自动抓取，且数据完整度和精度远超人工方式。

#### 4.1.2 协议支持

| 协议 | 国际标准 | 用途 | 典型数据来源 | 传输层 |
|------|---------|------|-------------|--------|
| **Modbus TCP** | IEC 61158 | 读取逆变器、电表、PLC 的寄存器数据 | 逆变器 / 电表 / PLC / 环境监测仪 | TCP/IP 端口 502 |
| **IEC 61850** | IEC 61850 | 读取保护装置、智能电子设备(IED)的结构化数据模型 | 保护装置 / IED / 合并单元 / 故障录波器 | MMS (Manufacturing Message Specification) |
| **OPC UA** | IEC 62541 | 读取 DCS、PLC、SCADA 上位机的统一架构数据 | DCS / PLC / SCADA 上位机 / 功率预测系统 | TCP/IP + UA 二进制编码 |

#### 4.1.3 数据采集流程

**第一步：告警触发**

当 SCADA 系统检测到设备异常告警时，告警消息通过 API 或消息队列推送至 驭能系统。告警消息包含设备标识、告警类型、告警级别和告警时间戳。

**第二步：协议适配**

系统根据设备类型自动选择对应的协议适配器。适配器负责建立物理连接、发送读取指令、接收数据响应。对于 Modbus TCP 设备，适配器通过 pymodbus 库读取指定寄存器地址的数值；对于 IEC 61850 设备，适配器通过逻辑节点和数据对象路径读取结构化数据；对于 OPC UA 设备，适配器通过节点 ID 订阅数据变化。

**第三步：数据标准化**

不同协议返回的数据格式各异。标准化层将所有数据统一转换为内部标准格式，包含以下字段：时间戳（毫秒精度）、设备 ID、测点名称、数值、单位、数据质量标记。这是全系统共用的统一数据模型。

**第四步：环形缓冲区存储**

标准化后的数据写入环形缓冲区。缓冲区采用定长循环队列实现，滚动存储最近 30 分钟的全量时序数据。当新数据到达时，最旧的数据自动被覆盖。以 1000 个测点、每秒采样一次的场站为例，环形缓冲区容量约为 180 万条记录（1000 点 × 60 秒 × 30 分钟）。

**第五步：故障窗口提取**

告警触发后，故障窗口提取器以告警时刻为中心，从环形缓冲区中提取前后各 5 分钟的数据窗口（共 10 分钟）。窗口内的数据按时间戳排序，形成连续的时序序列。

**第六步：注入诊断引擎**

提取的故障窗口数据以文本摘要和统计特征两种形式注入 Diagnosis 子图。文本摘要包括各参数的 min/max/mean/std/trend/slope 统计量；统计特征包括趋势方向（上升/下降/平稳）、变化率、异常点标记。

#### 4.1.4 价值量化

| 指标 | 传统人工方式 | SCADA 连接器 |
|------|------------|-------------|
| 数据获取速度 | 人工登录 SCADA + 截图 + 描述 = 1-3 分钟 | 自动抓取 < 3 秒 |
| 数据完整性 | 依赖人工选择，常遗漏关键参数 | 全量测点自动获取，无遗漏 |
| 数据精度 | 人眼读数 ± 主观判断 | 毫秒级精度，连续采样 |
| 自动化程度 | 半自动（人工触发、人工输入） | 全自动（告警触发即诊断） |

### 4.2 Judge Agent（诊断质量评估系统）

#### 4.2.1 功能概述

Judge Agent 是系统中的一个独立裁判智能体。它不对故障本身做判断，而是对 DiagnosisAgent 的诊断报告进行质量评估。它的核心使命是回答一个问题：**"这份诊断报告，有多大把握是对的？"**

#### 4.2.2 五维度评分体系

Judge Agent 从五个维度对诊断报告进行量化评分，每个维度满分 100 分，最终加权计算总分：

| 评估维度 | 权重 | 评分依据 | 典型扣分场景 |
|----------|------|---------|-------------|
| **证据充分性** | 25% | 证据来源数量是否≥2个；不同来源的证据是否相互印证（而非矛盾）；证据是否覆盖故障的所有关键方面 | 仅依赖单一来源（如只靠 LLM 推理无工具数据支撑）；证据间存在矛盾（如 SCADA 显示正常但日志显示异常） |
| **推理逻辑性** | 25% | 因果链条是否完整（从现象到根因有清晰路径）；是否考虑了替代假设（而非只给出一种可能）；推理过程中是否有逻辑跳跃或循环论证 | 因果倒置（如说"停电导致线路故障"，实际是线路故障导致停电）；未排除明显替代假设 |
| **安规合规性** | 20% | 处置方案是否符合电力安规要求；是否引用了具体的安规条款编号（而非泛泛而谈"注意安全"）；高风险操作是否标注了⚠️并建议人工确认 | 建议"直接带电操作"而未提验电步骤；未引用具体安规条款；高风险操作未警告 |
| **可操作性** | 20% | 处置步骤是否具体、可执行（而非"检查设备"这种笼统描述）；是否明确了责任人/所需工具/预计耗时/操作顺序；步骤之间的前后依赖关系是否清晰 | "检查逆变器" → 应改为"打开逆变器柜门，使用万用表测量A相输入端电压"；缺少工具清单 |
| **历史一致性** | 10% | 诊断结论是否与同设备的历史诊断保持一致性（同类故障应有相似根因）；是否与行业标准和厂家手册一致 | 对同一设备的重复故障给出了不同的根因；诊断与厂家技术手册矛盾 |

#### 4.2.3 评分等级与处理策略

| 分数区间 | 等级 | 等级含义 | 处理策略 |
|----------|------|---------|---------|
| **90-100** | A（优秀）| 诊断结果高度可信，证据充分、逻辑严密、合规可操作 | 直接输出报告，无需任何额外操作 |
| **80-89** | B（良好）| 诊断结果可信，个别维度略有不足但不影响整体判断 | 直接输出报告，标注置信度 |
| **70-79** | C（合格）| 诊断结果基本可信，但存在一些不确定性，建议人工复核 | 输出报告 + 标注"建议人工复核" + 列出不确定性来源 |
| **60-69** | D（待改进）| 诊断结果可信度不足，存在明显缺陷 | 自动触发重规划（第 1 次）：扩大检索范围、重新推理 |
| **< 60** | F（不合格）| 诊断结果不可信，存在严重错误或信息严重不足 | 自动触发重规划（第 2 次）；仍不合格则降级为人工介入模式 |

#### 4.2.4 闭环价值

Judge Agent 不只是一个评分工具，它是系统质量保障的核心机制：

- **质量兜底**：低分自动触发重规划，确保最终输出的诊断报告始终达到 C 级以上
- **信任建立**：用户看到的不仅是诊断结论，还有量化的可信度评分，不再"盲猜"AI 是否准确
- **持续优化**：评分数据积累形成质量趋势报告，可以识别哪些类型的故障、哪些设备类型诊断准确率偏低，驱动模型和知识库的定向优化
- **人工协同**：当自动诊断确实无法达到阈值时，系统主动降级为人工介入，而非强行输出不可信的结果

### 4.3 多模态数据融合诊断模块

#### 4.3.1 功能概述

传统设备诊断仅依赖文本信息（告警描述、日志文字），但很多故障需要图像和声音才能准确判断。多模态融合诊断模块支持**文本 + 图像 + 声音**的联合分析——运维人员拍摄现场照片或红外热像图，系统自动分析图像中的异常区域，同时如果上传了设备运行声音录音，系统也能识别其中的故障声音特征。所有模态的分析结果最终融合为一个统一的诊断输入。

#### 4.3.2 模态能力详解

**文本模态**

文本是诊断的基础模态，承载用户的故障描述、SCADA 告警文本、设备日志等。DeepSeek V4 Pro 负责文本的语义理解、实体提取和意图识别。文本模态的输出是结构化的故障特征向量和实体字典。

**图像模态**

图像模态支持三种类型的图片分析：

- **红外热像图**：通过 Qwen-VL-Max 分析温度分布，自动检测温度异常区域（比周围环境高 10°C 以上的热点），输出异常区域的坐标、温度值和面积。对于逆变器，重点关注 IGBT 模块区域；对于变压器，重点关注套管和散热器区域。
- **现场可见光照片**：识别设备外观异常——变形、裂纹、腐蚀、污秽、异物附着。对于风机叶片，可识别表面裂纹和雷击痕迹；对于光伏组件，可识别玻璃破裂和边框变形。
- **频谱/波形图**：分析振动频谱图和电流波形图，识别特征频率和波形畸变，辅助判断机械故障类型（不平衡/不对中/轴承故障/齿轮故障）。

**声音模态**

声音模态通过 Audio Spectrogram Transformer（AST）模型分析设备运行声音。AST 将音频转换为梅尔频谱图，然后用 Vision Transformer 架构进行分类。可识别的故障声音特征包括：轴承干摩擦的高频嘶嘶声、齿轮断齿的周期性撞击声、电弧放电的噼啪声、变压器铁芯松动的嗡嗡声。

#### 4.3.3 多模态融合架构

融合采用**Cross-Attention 交叉注意力机制**，以文本模态为主体，图像和音频模态作为补充：

1. **分模态特征提取**：三个模态分别通过各自的编码器提取特征向量。文本通过 DeepSeek V4 Pro → 2048 维向量；图像通过 Qwen-VL-Max Vision Encoder → 1024 维向量；音频通过 AST → 768 维向量。

2. **维度对齐**：图像特征和音频特征分别通过线性投影层映射到与文本相同的 2048 维空间，使得不同模态的特征可以在同一维度下进行注意力计算。

3. **跨模态注意力**：以文本特征作为 Query，图像特征和音频特征作为 Key 和 Value，计算文本对图像的注意力权重和文本对音频的注意力权重。这相当于让文本"关注"图像中的哪些区域，以及声音中的哪些频段。

4. **特征融合**：将文本原始特征、文本→图像注意力输出、文本→音频注意力输出进行拼接，通过一个全连接层降维到 2048 维的融合特征向量。

5. **注入诊断**：融合特征向量以结构化描述文本的形式注入 DiagnosisAgent 的上下文，作为多源证据的一部分参与综合推理。

#### 4.3.4 价值对比

| 维度 | 纯文本诊断 | 多模态融合诊断 |
|------|-----------|---------------|
| 信息利用率 | 低，只能处理文字信息 | 高，图像 + 文字 + 声音联合分析 |
| 诊断速度 | 需人工输入所有观察信息 | 拍照上传即可自动分析 |
| 诊断精度 | 依赖人工描述的准确性和完整性 | 图像客观数据消除了主观偏差 |
| 异常覆盖率 | 仅靠文字描述容易遗漏隐性异常 | 红外热像可发现肉眼不可见的早期发热 |
| 证据维度 | 单一文本来源 | 文本 + 视觉 + 听觉三重证据交叉验证 |

> **行业基准**：华电智"御能"电力安全生产大模型的多模态设备异常识别精度已超过 85%，本系统采用同等技术路线，预期达到同等或更高水平。

### 4.4 主动学习与自我进化系统

#### 4.4.1 功能概述

驭能区别于传统诊断系统的最核心特征是其**持续学习能力**。系统不是部署好就一成不变的，而是每一次成功的诊断都会沉淀为新的知识，每一次被纠正的错误都会驱动改进。主动学习方法在电力领域的实践表明，它可以有效降低模型训练所需标注数据量 60% 以上，同时提升故障辨识精度 10% 以上。

#### 4.4.2 学习机制

| 学习机制 | 触发条件 | 执行动作 | 存储位置 | 知识形式 |
|----------|---------|---------|---------|---------|
| **成功案例入库** | 用户反馈"准确"（通过 /api/feedback 接口） | 将本次诊断的完整文本（故障描述 + 根因 + 处置方案）通过 BGE-Large-ZH 编码为 1024 维向量，存入 ChromaDB 长期记忆集合 | ChromaDB collection: long_term_memory | 向量 + 结构化元数据（设备类型、故障类型、时间戳、置信度、操作人员） |
| **Skill 自动生成** | 同一故障模式在长期记忆中累积出现 ≥ 3 次 | LLM 分析 3 个相似案例的共性特征，提炼为标准化 Skill——包含诊断流程、关键检查点、常见根因列表、推荐处置步骤 | SkillRegistry（内存 + JSON 持久化） | 场景化 Prompt 模板 + 关联工具列表 |
| **反馈驱动优化** | 用户反馈"部分准确"或"不准确" | 将用户的修正信息与原始诊断结果绑定，存入待审核池。运维专家定期审核，"部分准确"的修正信息直接合并为 Skill 优化，"不准确"的案例标记为负样本 | 待审核 Skill 池（JSON 文件） | 修正标注 + 正/负样本标记 |
| **模型增量微调** | 积累 ≥ 50 个新标注案例（正样本 + 负样本） | 触发 LoRA（Low-Rank Adaptation）增量微调：冻结 DeepSeek 原始权重，仅训练低秩适配矩阵，参数量仅为全量微调的 0.1%，可在单卡 GPU 上完成 | 模型权重文件（adapter_model.bin） | LoRA 适配器权重，不影响原始模型 |

#### 4.4.3 学习闭环

学习闭环的完整链路为：

- **诊断完成** → 系统输出诊断报告
- **用户反馈** → 通过前端界面或 API 提交反馈，三个选项：准确 / 部分准确 / 不准确
- **准确** → 诊断文本向量化存入 ChromaDB → 检查同一故障模式是否 ≥ 3 次 → 是则自动生成 Skill
- **部分准确** → 用户标注修正信息（指出哪里对、哪里错）→ 存入待审核池 → 专家定期审核
- **不准确** → 记录失败案例 → 标记为负样本
- **积累检查** → 新标注案例（正样本 + 负样本）累积 ≥ 50 个 → 触发 LoRA 增量微调 → 更新模型 → 部署新版本

#### 4.4.4 预期价值

| 指标 | 部署初期 | 运行 3 个月后 | 运行 12 个月后 |
|------|---------|-------------|--------------|
| 诊断准确率 | 85%（依赖通用知识） | 90%（积累场站特定知识） | 95%（深度适配本场站） |
| 一次诊断成功率（无需重规划） | 70% | 82% | 90% |
| 人工介入率 | 30% | 18% | 10% |
| Skill 自动生成数量 | 3（初始内置） | 10-15 | 30-50 |
| 知识库案例数量 | 0（场站特定） | 200+ | 1000+ |

---

## 五、知识库与 RAG 检索层

### 5.1 整体架构

知识库与 RAG 检索层分为**建库**和**检索**两条独立链路：

**建库链路**：原始文档（PDF/Word/Excel/TXT/扫描件）→ 文档解析（版面分析/文本提取/表格提取/图片提取/OCR）→ 知识结构化（章节分割/元数据标注/实体抽取）→ 向量化（BGE-Large-ZH，1024 维）→ 多模态存储（ChromaDB 向量库 + Neo4j 图谱(可选) + MinIO 原始文档）

**检索链路**：用户查询 → Query 改写 → 三重混合检索（向量 + BM25 + 知识图谱）→ RRF 融合排序 → BGE Reranker 交叉编码精排 → 知识图谱上下文扩展 → 注入诊断引擎

### 5.2 PDF 知识库构建流程

| 阶段 | 任务 | 关键技术 | 产出 |
|------|------|---------|------|
| **阶段一：文档采集** | 收集电力行业相关文档 | API 接口批量上传 + 手动上传 | 原始文档存储 |
| **阶段二：文档解析** | 提取文档的完整结构化内容 | PyMuPDF（文本PDF）、PaddleOCR（扫描件，>95%准确率）、TableBank（表格识别）、LayoutParser（版面分析） | 文本段落 + 表格数据 + 图片 + 文档层级树 |
| **阶段三：知识结构化** | 将文档内容转换为机器可检索的知识单元 | 按标题层级（H1/H2/H3）切分 chunk、元数据标注（来源/版本/日期）、实体抽取（设备名称/故障类型）、向量化（BGE-Large-ZH） | 结构化 Chunk（500-800 字符/段，100 字符上下文重叠） |
| **阶段四：多模态存储** | 将结构化知识存入多种存储引擎 | ChromaDB（向量存储，支持 HNSW 索引）、Neo4j（图存储，可选）、MinIO/S3（原始文档归档） | 三重索引：向量索引 + 关键词索引 + 图谱索引 |

文档来源包括：国家电网电力安全工作规程（变电部分/线路部分）、宁夏电网调度规程、设备厂家技术手册（金风/远景/明阳/华为/阳光电源）、历史故障案例分析报告、行业标准（IEC 61400 / GB/T 19963 等）。

### 5.3 混合检索技术

系统采用**三重检索 + RRF 融合 + 重排序**的级联检索策略：

**第一重：向量检索**

技术：ChromaDB 内置 HNSW（Hierarchical Navigable Small World）索引 + BGE-Large-ZH 嵌入模型。

原理：将查询文本编码为 1024 维向量，在 ChromaDB 的高维向量空间中搜索余弦距离最近的文档片段。HNSW 是当前最快的近似最近邻搜索算法之一，在百万级向量规模下检索延迟 < 10ms。

特点：擅长语义相似度匹配，能够找到"用词不同但意思相同"的内容。例如查询"风机为什么会停下来"能匹配到"风电机组紧急停机原因分析"。

**第二重：关键词检索**

技术：BM25 算法（Okapi BM25，k1=1.5, b=0.75）+ 电力领域专用词典（17 个关键词：电压、电流、功率、频率、温度、振动、故障、告警、逆变器、风机、变压器、断路器、保护、接地、短路、过载、通讯中断、绝缘、IGBT、PLC、SCADA、齿轮箱、叶片、偏航、变桨、直流侧、交流侧、并网、脱网、低电压穿越、高电压穿越、AGC、AVC、无功补偿、有功功率、箱变、集电线路、SVG）。

原理：将文档和查询分别分词，计算每个词项的 TF（词频）和 IDF（逆文档频率），加权求和后排序。BM25 对文档长度做了归一化处理，避免了长文档天然的 TF 优势。

特点：擅长精确匹配，适合专业术语和设备编号查询。例如查询"INV001 IGBT 温度"能精确命中包含这些关键词的文档。

**第三重：知识图谱检索**

技术：实体匹配 + 一跳路径搜索。

原理：首先从查询中提取设备实体（如"逆变器"），然后在知识图谱中查找该设备关联的故障节点（如"逆变器"→"IGBT过温"→"散热不良"），返回故障链路上的所有关联知识。

特点：擅长因果推理。例如查询"逆变器绝缘阻抗低"，知识图谱能返回"原因：组件绝缘降低 / 直流电缆老化 / 潮湿环境 → 排查：绝缘电阻测试 / 外观检查 / 红外扫描 → 安全：直流侧断开、电容放电后操作"的完整因果链路。实测可提升诊断准确率 6.56% 以上。

**RRF 融合算法**

三种检索方式各自返回一个排序列表。RRF（Reciprocal Rank Fusion，倒数排名融合）将这些列表融合为一个统一排序。

核心公式：对于每个文档 d，融合分数 = Σ( weight_source / (k + rank_source(d)) )，其中 k 是平滑常数（设 60），防止个别 0 排名产生除零。权重配置：向量检索 1.0、关键词检索 1.0、知识图谱检索 0.8。

**BGE Reranker 精排**

RRF 融合后的 Top-K（K=20）候选结果交由 BGE-Reranker-v2-m3 进行交叉编码精排。与向量检索的"双塔模型"不同，交叉编码器将查询和文档拼接后完整送入 Transformer，进行更深层的语义交互。精排后取最终 Top-5 结果作为 RAG 上下文。

### 5.4 知识图谱覆盖

知识图谱覆盖新能源场站 **10 大设备类别 × 48 种设备类型**，每种设备包含告警类型、常见原因、排查步骤、安全事项四个维度的完整知识。

#### 5.4.1 覆盖总览

| 类别 | 设备类型 | 数量 | 代表性设备 |
|------|---------|------|-----------|
| **风力发电设备** | 风电机组、齿轮箱、发电机、偏航系统、变桨系统、叶片、主轴轴承、机舱 | 8 | 金风 GW 3.0MW / 远景 EN-156 / 明阳 MySE |
| **光伏发电设备** | 光伏组件、光伏阵列、汇流箱、直流配电柜、交流配电柜 | 5 | 隆基 Hi-MO / 华为 SUN2000 逆变器 |
| **储能系统设备** | 储能 PCS（变流器）、电池舱、BMS（电池管理系统） | 3 | 宁德时代电池舱 / 阳光电源 PCS |
| **变电一次设备** | 主变压器、GIS 组合电器、断路器、隔离开关、电流互感器(CT)、电压互感器(PT)、避雷器 | 7 | 西电变压器 / ABB 断路器 / 南瑞继保 |
| **无功补偿设备** | SVG 静止无功发生器、SVC 静止无功补偿、电容器组 | 3 | 思源电气 SVG / 荣信 SVC |
| **线路设备** | 集电线路、电缆终端、架空线路、光缆线路 | 4 | 35kV 集电线路 / 110kV 送出线路 |
| **保护与测控设备** | 线路保护装置、主变保护装置、母线保护装置、故障录波器、PMU 同步相量测量单元 | 5 | 南瑞 RCS-931 / 四方 CSC-103 |
| **站用电与辅助设备** | 站用变压器、直流系统、UPS 不间断电源、消防系统、环境监测系统 | 5 | 站用变 SCB10 / 艾默生 UPS |
| **系统与控制设备** | AGC 自动发电控制、AVC 自动电压控制、SCADA 监控系统、功率预测系统 | 4 | 南瑞 OPEN-3000 / 金风 SCADA |
| **通讯设备** | 交换机、路由器、通讯管理机、远动装置(RTU) | 4 | 华为交换机 / 四方通讯管理机 |
| **合计** | | **48** | |

#### 5.4.2 风力发电设备详细故障模式

| 设备 | 告警类型 | 常见原因 | 排查步骤 | 安全注意事项 |
|------|---------|---------|---------|-------------|
| **风电机组** | 振动超标、齿轮箱温升异常、偏航系统故障、变桨角度异常、风速仪故障、发电机过热、塔筒倾斜 | 齿轮箱润滑油不足/劣化、轴承磨损/点蚀、偏航电机故障、叶片结冰/不平衡、变桨电池老化、风速仪冰冻 | ① 振动频谱分析（判断不平衡/不对中/轴承故障）② 内窥镜检查齿轮啮合面 ③ 检查偏航制动器间隙 ④ 外观检查叶片（望远镜/无人机）⑤ 风速仪加热器功能测试 | 风机停机并机械锁定；进入轮毂前必须确认机械刹车已施加；高空作业全程系双钩安全带；风速 > 10m/s 禁止登塔 |
| **齿轮箱** | 油温过高、油压异常、振动超标、金属颗粒超标、油位异常 | 润滑油劣化（氧化/乳化）、轴承点蚀/剥落、齿轮断齿/点蚀、冷却系统风扇故障、油泵故障、密封件老化漏油 | ① 取油样送实验室化验（粘度/酸值/水分/金属元素光谱分析）② 内窥镜检查齿轮和轴承滚道 ③ 振动频谱分析（识别齿轮啮合频率边带）④ 检查冷却风扇和散热器 ⑤ 检查油滤和更换周期 | 停机状态下检查；刚停机时油温高，防烫伤；换油时注意废油回收 |
| **发电机** | 绕组温度过高、轴承温度过高、绝缘电阻降低、振动异常、集电环打火 | 冷却风道堵塞（柳絮/沙尘）、轴承润滑脂不足/劣化、绝缘老化（热老化/电老化）、三相不对称运行、碳刷磨损 | ① 绝缘电阻测试（2500V 兆欧表，对地和相间）② 直流电阻测试（检查绕组是否断股）③ 红外热像扫描（找局部过热点）④ 振动分析（区分电磁振动和机械振动）⑤ 清理冷却风道滤网 | 停电验电放电后操作；测量绝缘前确认无残余电压；集电环检查需在停机状态 |
| **偏航系统** | 偏航电机过载、偏航制动器磨损、偏航角度偏差、偏航异响、偏航速度异常 | 偏航制动器摩擦片磨损超标、偏航电机轴承卡涩、偏航齿轮啮合间隙过大/过小、偏航轴承润滑不良、偏航编码器故障 | ① 检查偏航制动器摩擦片厚度（低于 5mm 需更换）② 测量偏航电机三相电流是否平衡 ③ 检查偏航齿轮啮合间隙（塞尺测量）④ 偏航轴承加注润滑脂 ⑤ 偏航编码器零位校准 | 偏航操作前确认周边无人员；锁定偏航制动器后进行机械检查；大风天气禁止偏航检修 |
| **变桨系统** | 变桨电机故障、变桨角度不一致（>1°偏差）、变桨电池电压低、变桨通讯故障、变桨速度异常 | 变桨电机碳刷磨损到极限、变桨电池老化（容量<80%）、变桨编码器故障/污染、滑环接触不良（碳粉堆积）、变桨驱动器故障 | ① 变桨电池组电压和内阻测试 ② 碳刷长度检查（小于新品 1/3 更换）③ 变桨编码器清洁和校准 ④ 滑环清洁（无水酒精擦拭）⑤ 变桨驱动器故障码读取 | 进入轮毂前必须锁定机械刹车；轮毂内操作需两人配合；变桨电池更换注意正负极 |
| **叶片** | 叶片裂纹、叶片结冰、叶片不平衡、雷击损伤、叶片表面腐蚀 | 复合材料疲劳（运行 5 年以上）、覆冰环境（温度 -5~0°C + 湿度 > 85%）、雷击（接闪器损坏）、砂石撞击、紫外老化 | ① 外观检查（望远镜/无人机近距离拍摄）② 敲击检测（判断内部分层和脱粘）③ 红外热像扫描（找内部缺陷）④ 叶片称重平衡测试 ⑤ 接闪器和引下线导通测试 | 高空作业系安全带和防坠器；风速 > 10m/s 禁止上塔检查；雷雨天气禁止靠近风机 |

#### 5.4.3 光伏发电设备详细故障模式

| 设备 | 告警类型 | 常见原因 | 排查步骤 | 安全注意事项 |
|------|---------|---------|---------|-------------|
| **光伏组件** | 功率衰减超预期、热斑效应、隐裂、PID效应（电位诱导衰减）、接线盒烧毁 | 电池片光致衰减(LID)、遮挡（鸟粪/灰尘/积雪）、制造缺陷（焊接不良/电池片裂纹）、电位诱导衰减（高湿度高电压环境）、接线盒二极管击穿 | ① IV 曲线测试（I-V 4000 测试仪）② EL 检测（电致发光，检测隐裂和断栅）③ 红外热像扫描（0.5°C 精度，找热斑）④ 绝缘电阻测试（组件对地）⑤ 旁路二极管功能测试 | 白天操作必须戴绝缘手套和绝缘鞋；断开组串和直流开关后操作；组件表面温度可达 60°C+ |
| **逆变器** | 通讯中断、IGBT 过温、直流过压/欠压、绝缘阻抗低、输出功率异常、电网电压/频率异常、电弧故障 | 通讯模块（PLC/RS485/光纤）故障、散热风道堵塞或风扇故障、电网电压波动超出 LVRT/HVRT 范围、组件或直流电缆绝缘降低、IGBT 模块老化（运行 > 5 年） | ① 检查通讯线路（RS485 匹配电阻/光纤光功率）② 清理散热风道和更换滤网 ③ 测量电网侧电压和频率 ④ 绝缘电阻测试（直流侧对地）⑤ 逆变器故障码和历史记录下载分析 | 直流侧和交流侧均断开；电容放电至少 5 分钟后操作（检查直流电压 < 10V）；戴绝缘手套和护目镜 |
| **汇流箱** | 支路电流异常（某路电流为 0 或远低于其他路）、熔断器熔断、防反二极管击穿、箱内温度过高、IP 等级下降进水 | 组件故障或 MC4 接头松动导致支路断开、雷电感应过电压、二极管劣化（反向漏电流增大）、接线端子松动发热、密封条老化 | ① 逐路用钳形电流表测试直流电流 ② 红外热像扫描箱内各节点温度 ③ 断电后检查熔断器和二极管 ④ 接线端子紧固（标准力矩）⑤ 密封条检查和更换 | 先断开直流侧输入再打开汇流箱；测量确认无电压后操作；更换熔断器必须用同规格 |
| **直流配电柜** | 直流母线电压异常波动、绝缘降低报警、断路器跳闸、电弧故障报警 | 绝缘老化或破损（长期高低温循环）、接线松动导致接触电阻增大、直流电弧（接头松动/断线）、断路器老化误动 | ① 绝缘电阻测试（直流母线对地，>1MΩ）② 红外热像扫描所有连接点 ③ 目视检查电缆绝缘层和接线端子 ④ 断路器分合闸功能测试 ⑤ 电弧故障检测器功能测试 | 放电后操作（直流母线电容大，放电时间长）；戴绝缘手套和护目镜；禁止带负荷拉合刀闸 |

#### 5.4.4 变电一次设备详细故障模式

| 设备 | 告警类型 | 常见原因 | 排查步骤 | 安全注意事项 |
|------|---------|---------|---------|-------------|
| **主变压器** | 油温高告警、差动保护动作、瓦斯继电器动作、铁芯接地电流大、局放超标、油位异常 | 长期过载运行导致绝缘加速老化、冷却系统故障（风扇/油泵）、内部短路（匝间/相间）、变压器受潮（呼吸器硅胶失效）、铁芯多点接地、套管污秽闪络 | ① 检查油温表、油位计和瓦斯继电器窗口 ② 取油样化验：色谱分析（7 种气体）、微水、耐压、介损 ③ 红外热像扫描（本体/套管/散热器）④ 铁芯接地电流测试（< 100mA）⑤ 局放测试（超声波法 + 高频电流法） | 停电操作、验电、挂接地线、挂警示牌；取油样需用专用密闭容器；瓦斯继电器动作后禁止强行复归；保持防爆安全距离 |
| **GIS 组合电器** | SF6 气压低报警、局放异常、断路器拒动/误动、隔离开关卡涩、CT/PT 二次异常 | SF6 气体泄漏（密封圈老化/法兰螺栓松动）、盆式绝缘子表面缺陷（制造或安装损伤）、操作机构卡涩（润滑不良/锈蚀）、控制回路断线或接触不良 | ① SF6 检漏（定量检漏仪，灵敏度 1ppm）② 局放测试（特高频法 UHF + 超声波法 AE）③ 操作机构润滑保养和机械特性测试 ④ 控制回路接线检查和绝缘测试 ⑤ SF6 气体微水和纯度测试 | SF6 泄漏气体比空气重，注意低处通风；进入 GIS 室前需通风 15 分钟；操作机构检查需断开控制电源 |
| **断路器** | 拒动/误动、SF6 气压低、储能电机故障（储能时间 > 15 秒）、触头温度过高、分合闸线圈烧毁 | 操作机构机械故障（卡涩/变形）、SF6 泄漏、控制回路断线、主触头烧蚀或接触不良、储能电机碳刷磨损、辅助开关切换不到位 | ① 分合闸时间/速度/同期性测试（断路器特性测试仪）② 回路电阻测试（主回路 < 120μΩ）③ SF6 检漏和微水测试 ④ 操作机构检查（润滑/紧固/间隙测量）⑤ 触头磨损量检查（内窥镜） | 断路器两侧均断开并验电；分合闸操作前确认安全范围内无人员；SF6 断路器操作后 10 分钟内禁止靠近排气口 |
| **避雷器** | 泄漏电流增大（> 正常值 2 倍）、计数器频繁动作、绝缘降低、底座绝缘不良 | 氧化锌阀片老化（运行年限 > 10 年）、受潮（密封不良进潮气）、大气过电压频繁冲击、污秽闪络 | ① 泄漏电流测试（阻性分量 < 0.3mA）② 绝缘电阻测试（2500V 兆欧表）③ 红外热像测温（本体温度应均匀，局热温差 > 1°C 为异常）④ 计数器动作次数记录 ⑤ 底座绝缘电阻测试（防反击） | 雷雨天气禁止靠近避雷器（至少保持 5 米）；测试前需断开与线路连接 |

#### 5.4.5 储能及补偿设备详细故障模式

| 设备 | 告警类型 | 常见原因 | 排查步骤 | 安全注意事项 |
|------|---------|---------|---------|-------------|
| **储能 PCS** | IGBT 过温、直流过压/欠压、交流过流、通讯故障、绝缘故障、并网失败 | 散热风道堵塞或空调故障、电池侧电压波动超出范围、电网扰动（电压暂升/暂降）、IGBT 模块老化（热循环 > 10000 次）、PCS 控制板故障 | ① 温度巡检（IGBT 模块/电抗器/变压器）② 直流侧电压检测和纹波测试 ③ IGBT 门极驱动测试 ④ 绝缘检测（直流侧/交流侧分别对地）⑤ PCS 故障日志下载分析 | 交直流双侧均断开；直流母线电容放电至少 10 分钟；电池侧带高压电，操作需戴绝缘手套和护目镜 |
| **电池舱** | 电芯温度过高（> 45°C）、电芯电压不均衡（> 50mV）、SOC 异常跳跃、绝缘降低报警、消防系统报警 | 热管理系统故障（空调/液冷）、BMS 采集模块故障、电芯容量衰减不一致、外部短路或漏电、电解液泄漏 | ① 电芯单体电压和温度逐通道巡检 ② 绝缘电阻测试（电池簇对地 > 10MΩ）③ 消防系统检查（气体灭火/水消防）④ 冷却系统检查（空调/液冷管路）⑤ BMS 通讯和故障码检查 | 电池舱内存在高压电和火灾爆炸风险；进入前必须通风 10 分钟；穿戴防护装备（绝缘手套/护目镜/防静电服）；严禁携带金属物品进入 |
| **SVG 无功补偿** | 功率模块故障、IGBT 过温、直流过压、交流过流、通讯故障、并网异常、谐波超标 | 功率模块 IGBT 老化、散热风机故障、控制板故障、电网谐波含量超标（THD > 5%）、SVG 控制参数不当 | ① 功率模块逐个检查（直流电压/温度/故障码）② 散热系统检查（风机转速/滤网/空调）③ 电网谐波测试（THD 和各次谐波含量）④ SVG 控制参数校核（比例/积分系数）⑤ 通讯光纤检查（光功率和端面清洁） | SVG 运行时高压带电，禁止靠近功率模块；检修前必须断开高压电源并放电至少 10 分钟；功率模块更换需等电容放电完成 |

#### 5.4.6 保护与测控设备详细故障模式

| 设备 | 告警类型 | 常见原因 | 排查步骤 | 安全注意事项 |
|------|---------|---------|---------|-------------|
| **线路保护装置** | 保护误动/拒动、通讯中断、采样值异常、定值校验失败、装置自检告警 | CT/PT 二次回路断线或接触不良、光纤通道误码率高、保护板卡（CPU/AD/出口板）故障、定值设置与定值单不一致、对时异常 | ① 采样值检查（电压/电流幅值和相角与一次设备一致）② 光纤通道误码率测试（< 10^-6）③ 保护定值逐项核对（与定值单对比）④ 保护功能传动试验 ⑤ 装置对时检查（GPS/北斗） | 传动试验需退出相关保护压板并确认不影响运行设备；修改定值需经调度许可并核对；CT 二次回路严禁开路 |
| **故障录波器** | 录波启动失败、存储空间满、时间同步丢失、录波数据不完整 | 触发条件设置不合理（门槛过高/过低）、硬盘存储满未清理、GPS/北斗天线故障或信号弱、模拟量/开关量通道故障 | ① 检查录波触发条件设置是否合理 ② 存储空间清理（保留至少 20% 余量）③ GPS/北斗天线检查和信号强度测试 ④ 录波通道测试（注入测试信号） | 不影响一次设备运行；录波器维护操作不影响保护功能 |
| **PMU 同步相量** | 时间同步丢失（> 26.5μs 偏差）、数据中断、相角测量偏差 > 1° | GPS/北斗天线故障、通讯通道中断（网络故障）、PMU 算法参数错误、CT/PT 通道故障 | ① GPS/北斗天线同轴电缆检查和信号强度测试 ② 网络通讯检查（ping 和带宽测试）③ 相角校准（与已知参考相量对比）④ PMU 校时模块检查 | 不影响一次设备运行；天线安装位置需确保对空视野开阔 |

#### 5.4.7 通讯及控制设备详细故障模式

| 设备 | 告警类型 | 常见原因 | 排查步骤 | 安全注意事项 |
|------|---------|---------|---------|-------------|
| **交换机** | 端口 down、环路告警（广播风暴）、电源模块故障、SFP 光模块光功率低 | SFP 光模块老化（光功率衰减）、网线/光纤物理损伤、电源模块故障、RSTP 环网配置错误、VLAN 配置不一致 | ① 端口状态和收发字节数检查 ② SFP 光功率测试（接收功率 < 灵敏度 -3dB 需更换）③ 电源模块指示灯和输入输出检查 ④ VLAN 和 RSTP 配置核对 ⑤ 备用交换机的配置和测试 | 不影响一次设备运行；更换光模块前确认端口无重要业务；光纤连接注意端面清洁 |
| **通讯管理机** | 通讯中断、规约转换异常、数据死区（长时间无更新）、缓存溢出、设备掉线 | 串口/网口故障、规约参数配置错误（波特率/数据位/校验/站地址）、下游设备掉线或故障、数据量超出处理能力 | ① 串口自环测试（确认串口硬件正常）② 规约报文抓包分析（确认帧格式和时序正确）③ 下游设备在线状态逐个检查 ④ 缓存区使用率检查 ⑤ 管理机 CPU 和内存使用率检查 | 不影响一次设备运行；修改规约配置前确认下游设备通讯将中断；操作前备份现有配置 |
| **远动装置 RTU** | 与调度主站通讯中断、遥测数据不刷新、遥信变位不推送、SOE 事件丢失 | 104 规约通道故障（网络/路由）、RTU 板卡故障（AI/DI/DO/COM 板）、对时异常导致时标错误、数据配置表错误 | ① 104 规约通道测试（建立连接+总召唤+变化推送）② RTU 各板卡自检和指示灯检查 ③ 对时检查（与调度主站时差 < 1ms）④ SOE 记录完整性检查 ⑤ 数据配置表核对（点号/系数/偏移量） | RTU 维护需提前通知调度自动化值班；遥信传动试验需退出关联保护出口压板；修改配置后需做全数据核对 |

#### 5.4.8 图谱实体类型体系

- **设备实体（48 种）**：风机 / 齿轮箱 / 发电机 / 偏航系统 / 变桨系统 / 叶片 / 主轴轴承 / 机舱 / 光伏组件 / 光伏阵列 / 汇流箱 / 直流配电柜 / 交流配电柜 / 储能 PCS / 电池舱 / BMS / 主变压器 / GIS 组合电器 / 断路器 / 隔离开关 / 电流互感器 / 电压互感器 / 避雷器 / SVG / SVC / 电容器组 / 集电线路 / 电缆终端 / 架空线路 / 光缆线路 / 线路保护 / 主变保护 / 母线保护 / 故障录波器 / PMU / 站用变压器 / 直流系统 / UPS / 消防系统 / 环境监测 / AGC / AVC / SCADA / 功率预测 / 交换机 / 路由器 / 通讯管理机 / 远动装置

- **故障实体（50+ 种）**：通讯中断 / 温度过高 / 振动超标 / 绝缘降低 / 电压异常 / 电流异常 / 频率异常 / SF6 气压低 / SF6 泄漏 / 油色谱异常 / 局放超标 / 功率衰减 / 热斑效应 / 隐裂 / PID 效应 / IGBT 过温 / 熔断器熔断 / 保护误动 / 保护拒动 / 采样异常 / 时间同步异常 / 电芯电压不均衡 / SOC 异常 / 消防报警 / 齿轮箱异响 / 叶片裂纹 / 叶片结冰 / 偏航角度偏差 / 变桨角度不一致 / 金属颗粒超标 / 铁芯接地电流大 / 瓦斯继电器动作 / 差动保护动作 / 触头温度过高 / 储能电机故障 / 泄漏电流增大

- **操作实体（30+ 种）**：停电 / 验电 / 挂接地线 / 拆除接地线 / 红外测温 / 油色谱分析 / 取油样 / 振动频谱分析 / 内窥镜检查 / IV 曲线测试 / EL 检测 / SF6 检漏 / 局放测试 / 回路电阻测试 / 分合闸时间测试 / 泄漏电流测试 / 绝缘电阻测试 / 直流电阻测试 / 光纤 OTDR 测试 / 光功率测试 / 规约抓包分析 / 定值核对 / 传动试验 / 采样值检查 / 碳刷检查 / 编码器校准 / 滑环清洁 / 电池内阻测试 / 电芯巡检 / 消防系统测试

- **安全实体（20+ 种）**：绝缘手套 / 绝缘靴 / 绝缘垫 / 安全帽 / 高空安全带 / 双钩安全带 / 防坠器 / 验电器 / 携带型接地线 / 警示牌 / SF6 防护面罩 / 防毒面具 / 护目镜 / 防静电服 / 有限空间通风设备 / 气体检测仪 / 灭火器 / 消防沙 / 防爆工具 / 急救箱

### 5.5 文档解析能力

| 格式 | 解析能力 | 特殊处理 |
|------|---------|---------|
| **PDF（文本版）** | 全文提取、表格提取、图片提取、层级结构重建 | 通过字体大小和缩进自动重建文档树（H1 > 18pt, H2 > 15pt, H3 > 13pt），保留标题/段落/列表的完整层级关系 |
| **PDF（扫描版）** | OCR 文字识别、表格结构还原 | 使用 PaddleOCR（PP-OCRv4），中文识别准确率 > 95%，支持表格单元格检测和结构还原，可处理倾斜和畸变文档 |
| **Word (.docx)** | 全文提取、表格提取、图片提取 | 保留段落样式（标题/正文/列表/引用），标题自动映射为 Markdown 的 #/##/### 层级 |
| **Excel (.xlsx)** | 多 Sheet 表格提取、数据格式转换 | 自动识别表头行（首行加粗或有底色的行），数字/日期格式保留，空单元格用 null 填充，输出为标准结构化 JSON |

---

## 六、三层记忆系统

系统采用**三层记忆架构**，实现从短期到长期的完整记忆体系：

| 记忆层 | 存储内容 | 存储技术 | 生命周期 | 核心价值 |
|--------|---------|---------|---------|---------|
| **短期记忆** | 当前会话的完整对话历史（保留最近 3 轮完整的"用户提问 → 助手回答"对） | Python 内置的 OrderedDict（有序字典），基于内存存储，读写延迟 < 1ms | 会话期间（用户关闭浏览器或主动清除会话后释放） | 保持多轮对话的上下文连贯性。例如用户先问"3号逆变器是什么型号"，再问"它最近有没有故障"，系统能理解"它"指代 3 号逆变器 |
| **工作记忆** | 当前诊断任务的完整中间状态：实体提取结果、RAG 检索结果、4 个子 Agent 的执行报告、工具调用返回值、Judge 评分明细 | LangGraph StateGraph 内建的 35 个状态键，在节点间自动传递，支持持久化 Checkpoint | 单次诊断任务（任务结束后清空，但关键结果持久化到长期记忆） | 任务执行过程中的数据缓存和节点间信息传递。例如 Executor 节点的输出自动传递到 Diagnosis 节点，无需手动管理变量 |
| **长期记忆** | 所有被用户确认"准确"的诊断案例（向量化后的诊断文本 + 结构化元数据）、自动生成的 Skill 模板、历史故障统计信息 | ChromaDB 向量存储（集合: long_term_memory）+ LangGraph Checkpointer（SQLite 持久化） | 永久存储（除非管理员手动清理） | 跨会话和跨时间的知识复用。例如 2 个月前诊断过类似的逆变器通讯中断案例，当今天再次出现时，系统能检索到该案例作为参考。这是"越用越聪明"的技术基础 |

**长期记忆的读取与写入机制**：

- **写入时机**：用户通过 /api/feedback 接口反馈"准确"后，系统将本次诊断的完整文本（故障描述 + 诊断根因 + 处置方案）调用 BGE-Large-ZH 编码为 1024 维向量，存入 ChromaDB 的 long_term_memory 集合，同时附带元数据（device_type: 逆变器, fault_type: 通讯中断, timestamp: 2026-07-29T10:30:00, confidence: 0.92, operator: 张三）

- **检索时机**：每次新诊断任务的 RAG 检索环节，系统不仅检索"公共知识库"（安规/手册/规程），还同时检索"长期记忆"集合。通过故障关键词提取 → 向量检索 → 按时间衰减加权（越近的案例权重越高）→ 返回 Top-3 相似历史案例

- **时间衰减机制**：历史案例的检索权重随时间的推移而衰减。衰减公式为 weight = 1 / (1 + days_since / 180)，即半年前的案例权重降为原来的 50%，确保最新的案例（可能反映设备当前的真实状态）具有更高参考价值

---

## 七、MCP 工具层

### 7.1 什么是 MCP

MCP（Model Context Protocol）是由 Anthropic 发布的一种开放协议，用于标准化 AI 模型与外部工具、数据源之间的通信。它定义了一套统一的接口规范，使得任何实现了 MCP 协议的 AI 系统都能无缝调用任何 MCP 兼容的工具。

MCP 的核心设计理念是"一次编写，处处运行"——工具开发者只需按 MCP 规范编写一次工具，即可被所有支持 MCP 的 AI 平台使用。这类似于 USB 协议之于外设硬件，或 HTTP 协议之于 Web 服务。

### 7.2 6 个标准工具

系统内置 6 个 MCP 标准工具，覆盖新能源场站故障诊断的全流程数据需求：

| 工具名称 | 输入参数 | 输出内容 | 适用诊断场景 |
|----------|---------|---------|-------------|
| **get_device_status** | device_id（如 WT001 / INV001 / TRA001） | 设备实时运行参数快照：状态（running/warning/fault/stopped）、温度（°C）、振动（mm/s）、有功功率（kW）、电压（V）、电流（A）、频率（Hz）、厂区位置 | 参数异常分析：当告警涉及"温度过高"或"振动超标"时，调用此工具获取当前精确数值，与正常范围对比 |
| **get_alarm_history** | device_id + 可选时间范围（start_time/end_time）+ 可选数量上限（limit） | 设备历史告警记录列表：告警ID、告警编码（ALM-001 ~ ALM-008）、告警描述、严重级别（critical/high/medium）、时间戳、处理状态（active/cleared/acknowledged） | 告警趋势分析：判断当前告警是偶发还是频发，是否与历史告警存在规律性关联 |
| **get_device_logs** | device_id + 日志类型（operation/event/error）+ 可选数量上限（limit） | 设备运行日志列表：日志ID、日志类型、级别（INFO/WARN/ERROR）、日志内容、时间戳 | 故障时间线重建：按时间排序的日志序列，用于确定故障发生前设备的操作序列和状态变化 |
| **get_defect_tickets** | device_id + 可选工单状态（open/processing/closed/all） | 缺陷工单历史列表：工单ID、标题、详细描述、当前状态、优先级（critical/high/medium）、创建时间、负责人 | 历史方案参考：判断当前故障是否有历史相似案例，能否复用已验证的处理方案 |
| **search_safety_rules** | keyword（如"停电操作"、"验电"、"逆变器检修"）+ 可选数量上限（limit） | 匹配的安规条款列表：条款ID、章节名称（如"第3章 停电操作"）、条款完整内容、来源文件（如"国家电网电力安全工作规程（变电部分）"） | 安全合规审查：确保处置方案中的每一步操作都符合安规要求，自动引用条款编号 |
| **get_device_profile** | device_id | 设备台账信息：设备名称、设备类型、制造商（如"金风科技"/"阳光电源"）、型号、额定功率、安装日期、最近检修日期、累计运行小时数 | 设备基础信息查询：了解设备的厂家、规格、役龄等背景信息，辅助判断是否存在设计缺陷或批次问题 |

### 7.3 MCP 协议架构

系统的 MCP 工具层采用 Server-Client 架构：

- **MCP Server（工具服务器）**：作为独立的进程运行（端口 9901），通过 MCP 协议暴露 6 个工具。Server 的职责是接收 Client 的工具调用请求，转发给对应的工具函数执行，并将执行结果返回给 Client

- **MCP Client（工具调用方）**：LangGraph Agent 和 OpenCode 均可作为 MCP Client。当 Agent 需要查询设备数据时，通过 MCP 协议向 Server 发起调用请求，等待 Server 返回结果后继续执行

- **通信方式**：支持两种通信传输方式——stdio（标准输入输出，用于本地进程间通信）和 HTTP（用于跨网络调用，RESTful 风格）

### 7.4 工具注册与发现

工具采用注册制管理。每个工具在 ToolRegistry 中注册时需提供以下元信息：工具名称（唯一标识）、功能描述（自然语言，用于 LLM 自主选择和用户界面展示）、标签列表（用于关键词搜索和分类）、风险等级（LOW/MEDIUM/HIGH，用于权限控制）。

LLM 在需要调用工具时，先通过 list_tools 接口获取所有可用工具的列表和描述，然后根据当前任务需求自主选择最合适的工具并调用。这实现了工具的"即插即用"——新增工具只需注册即可被 LLM 发现和使用，无需修改编排逻辑。

---

## 八、API 接口总览

### 8.1 核心 API 列表

| 接口名称 | 路径 | HTTP 方法 | 功能概述 | 返回格式 |
|----------|------|-----------|---------|---------|
| **对话接口** | `/api/chat` | POST | 提交故障描述进行同步诊断，等待完整结果后返回 | JSON（含 session_id, answer, diagnosis） |
| **流式对话** | `/api/chat/stream` | POST | 提交故障描述进行流式诊断，通过 SSE 实时推送诊断进度和内容 | Server-Sent Events（逐字推送 + 诊断数据事件） |
| **诊断接口** | `/api/diagnose` | POST | 提交故障症状进行结构化诊断，返回 RootCause 列表 + 置信度 | JSON（DiagnosisResponse 模型） |
| **流式诊断** | `/api/diagnose/stream` | POST | 流式版本的诊断接口，推送进度通知 + 诊断内容 + 结构化诊断数据 | Server-Sent Events |
| **告警接收** | `/api/alarm/receive` | POST | 接收 SCADA 系统推送的告警消息，返回 task_id 用于后续诊断 | JSON（task_id, alarm_id, status） |
| **告警诊断** | `/api/alarm/diagnose` | POST | 基于已接收的告警消息触发自动诊断（SSE 流式输出） | Server-Sent Events |
| **诊断状态** | `/api/alarm/diagnose/{task_id}/status` | GET | 查询指定诊断任务的最新状态和进度 | JSON（task_id, status, diagnosis_result） |
| **检查点查询** | `/api/alarm/checkpoint/{task_id}` | GET | 查询诊断任务经过的所有检查点（用于进度追踪） | JSON（task_id, checkpoints 列表） |
| **文档上传** | `/api/knowledge/documents/upload` | POST | 上传知识文档（PDF/DOCX/XLSX/TXT/MD）到知识库 | JSON（document_id, filename, chunk_count, status） |
| **知识搜索** | `/api/knowledge/search/test` | POST | 测试知识库检索效果，返回 Top-K 结果及 RRF 评分 | JSON（query, total, results 列表） |
| **诊断回放** | `/api/trace/{task_id}/replay` | GET | 回放指定诊断任务的完整执行轨迹，含每个节点的输入/输出/耗时 | JSON（trace_id, steps 列表，每步含 node_name/input/output/elapsed） |
| **用户反馈** | `/api/feedback` | POST | 提交对诊断结果的评价（准确/部分准确/不准确）及修正意见 | JSON（status, message） |
| **SCADA 连接** | `/api/scada/connect` | POST | 配置 SCADA 连接参数（协议类型/地址/端口/测点列表） | JSON（status, connection_id） |
| **工具列表** | `/api/tools/list` | GET | 列出系统所有可用的 MCP 工具及其描述和标签 | JSON（tools 列表） |
| **工具搜索** | `/api/tools/search` | GET | 按关键词搜索工具（支持模糊匹配名称和描述） | JSON（匹配的工具列表） |
| **技能列表** | `/api/skills` | GET | 列出系统所有已注册的 Skill 及其触发意图 | JSON（skills 列表） |
| **健康检查** | `/health` | GET | 检查服务健康状态，返回模型名称和部署信息 | JSON（status, service, version, model） |

### 8.2 API 统一响应格式

所有 API 返回统一的 JSON 响应结构，包含三个字段：

| 字段 | 类型 | 说明 |
|------|------|------|
| code | integer | 状态码。0 表示成功，非 0 表示各类错误（400 参数错误、401 未认证、500 服务器错误等） |
| data | object/mixed | 业务数据。成功时包含请求的返回数据，失败时为 null |
| message | string | 提示信息。成功时为 "success" 或具体操作描述，失败时包含错误原因 |

### 8.3 SSE 流式输出协议

流式接口（/stream 后缀）使用 SSE（Server-Sent Events）协议，推送以下三种消息类型：

| 消息类型 | 格式 | 触发时机 | 示例内容 |
|---------|------|---------|---------|
| **start** | `data: {"type":"start","session_id":"xxx"}` | 诊断开始时 | 会话标识 |
| **status** | `data: {"type":"status","message":"正在分析症状..."}` | 每个关键步骤完成时 | "正在检索知识库..." / "正在执行子Agent并行分析..." / "正在进行综合诊断..." |
| **content** | `data: {"type":"content","text":"诊断报告内容片段..."}` | 诊断内容生成时，逐片段推送 | 诊断报告的实际文字内容，每片段 50-80 字符 |
| **diagnosis** | `data: {"type":"diagnosis","data":{...}}` | 结构化诊断结果可用时 | root_causes 列表、confidence 置信度、risk_level 风险等级 |
| **done** | `data: [DONE]` | 诊断完成时 | 流结束标记 |

### 8.4 安全性设计

| 安全机制 | 实现方式 | 覆盖范围 |
|---------|---------|---------|
| **XSS 防护** | PreRouteHook 在请求进入系统前移除所有 HTML 标签和 script 标签，正则过滤常见 XSS 载荷 | 所有 /api/chat 和 /api/diagnose 接口 |
| **敏感词过滤** | SafetyCheckHook 检查输入是否包含炸弹/武器/破坏/黑客等敏感关键词，命中直接拒绝 | 所有用户输入接口 |
| **输出脱敏** | PostToolUseHook 和 DataMaskingHook 对工具返回结果和最终输出中的手机号（1[3-9]\\d{9}）、邮箱、密码等敏感信息进行自动脱敏 | 所有输出内容 |
| **审计日志** | AuditHook 记录每次对话的用户 ID、意图类型、置信度、风险等级、操作时间 | 所有接口调用 |
| **会话管理** | 每次对话生成唯一 session_id，对话历史通过会话 ID 隔离，用户只能访问自己的会话 | 所有对话接口 |
| **高风险审批** | 风险等级为 CRITICAL 或 HIGH 的诊断结果，HumanApprovalHook 自动标记为需人工审批 | 高风险诊断场景 |

---

## 九、完整数据流

### 9.1 诊断完整路径

以下以一次典型故障诊断为例，展示数据从入口到出口的全链路流动。示例故障："#3 逆变器通讯中断，后台报 ALM-001 告警，运维人员同时上传了逆变器的红外热像图"。

**第一步：用户输入与多模态数据上传**

用户通过前端界面或 API 提交故障描述文本和一张红外热像图。系统自动创建 session_id 和 task_id，初始化状态字典。

**第二步：PreCheck（输入校验）**

PreRouteHook 执行输入清洗（去 HTML 标签、截断超过 5000 字符的部分）。SafetyCheckHook 执行敏感词检查（通过——不包含不安全内容）。输出：cleaned_input = "#3 逆变器通讯中断，后台报 ALM-001 告警"。

**第三步：ContextLoad（上下文加载）**

系统从三层记忆中加载上下文。短期记忆：当前会话的最近 3 轮对话历史（首次对话为空）。Skill 上下文：通过 SkillRegistry 匹配到 "power-fault-diagnosis" 技能，加载 7 步诊断流程 Prompt。工作记忆：创建空的任务状态空间。

**第四步：Supervisor（意图识别与路由）**

RouterAgent 调用 DeepSeek V4 Pro，输出意图分析结果：intent = FAULT_DIAGNOSIS，confidence = 0.95，entities = {device_type: "逆变器", device_id: "INV003", fault_keywords: ["通讯中断", "ALM-001"]}。PostRouteHook 校验意图合法性（通过）。IntentDispatcher 根据 FAULT_DIAGNOSIS 意图将请求路由到 Diagnosis 子图。

**第五步：SCADA 子图（并行）**

SCADA 数据连接器根据 device_id = INV003 自动查询设备类型为逆变器，选择 Modbus TCP 协议适配器，连接 SCADA 系统的 502 端口。以告警时刻为中心，从环形缓冲区提取前后各 5 分钟（共 10 分钟）的连续时序数据。输出统计特征：温度从 52°C 升至 85°C（趋势上升，斜率 0.15°C/s），有功功率从 480kW 骤降至 0kW（变化发生在告警前 2 分钟），电压和电流数据正常。

**第六步：多模态子图（并行）**

Qwen-VL-Max 分析红外热像图。模型识别到 IGBT 模块区域存在一个 15cm × 20cm 的热点区域，比周围环境温度高约 15°C。输出描述："逆变器 IGBT 模块区域存在局部过热，最高温度约 85°C，温度分布不均匀，疑似 A 相 IGBT 模块散热异常"。该描述作为多模态证据注入诊断上下文。

**第七步：Diagnosis 子图执行**

7.1 Entity Extract：提取结构化实体字典（同上）。

7.2 Alarm RAG：三重检索（向量 + BM25 + 知识图谱）。向量检索召回 3 个相似的历史逆变器通讯中断案例。知识图谱扩展返回逆变器故障模式：通讯中断的可能原因（通讯模块故障/散热不良/电网电压波动/组件绝缘降低）、排查步骤、安全注意事项。

7.3 Planner：LLM 制定诊断计划，包含 4 个步骤——① 检查设备实时参数是否异常、② 检索安规条款确保操作合规、③ 分析运行日志确定事件时间线、④ 查询历史工单判断是否为已知问题。

7.4 Executor：4 个子 Agent 并行执行。regulation 返回安规："通讯中断检修需确认直流侧断开，电容放电完毕后方可操作"。metrics 返回参数分析："温度 85°C 严重偏高（正常范围 35-65°C），功率骤降为 0，初步判断为保护性停机"。log 返回时间线分析："T-2min 温度开始快速上升 → T-30s 通讯模块发送告警报文 → T-0 逆变器主控触发保护停机"。ticket 返回工单匹配："历史相似案例 TK-202607001（通讯模块异常，因光纤接口松动导致，已修复）"。

7.5 Evidence Validation：证据数量 = 6（RAG 结果 + SCADA 数据 + 红外热像 + 4 个子 Agent 报告），证据间一致性检查通过（SCADA 温度数据与红外热像温度数据一致，日志时间线与 SCADA 数据时间线一致），覆盖度 = 0.85（覆盖了参数、时间线、历史、多模态四个维度）。

7.6 Diagnose：DeepSeek V4 Pro 综合所有证据，生成 9 项结构化诊断报告。根因判定：通讯中断的根本原因是 IGBT 模块过热导致逆变器主控保护停机，通讯中断是停机的结果而非原因。置信度 0.88。

7.7 Replanner：由于置信度 0.88 > 0.7，无需触发重规划。

7.8 Risk Assessment：风险等级 = HIGH（逆变器停机导致光伏阵列功率全部损失 500kW），影响范围 = 单台逆变器下的光伏阵列，紧急程度 = 需要尽快处理（但非危及人身安全）。

7.9 Action Recommend：生成处置方案——① 断开 INV003 直流侧和交流侧开关（安全：戴绝缘手套，确认电容放电）② 打开逆变器柜门，红外热像精确定位过热点 ③ 检查 IGBT 模块和散热器接触是否良好 ④ 检查散热风扇和风道是否堵塞 ⑤ 如 IGBT 模块损坏，更换新模块（预计耗时 2-3 小时）。

**第八步：Judge 子图评估**

从五个维度评分：证据充分性 90 分（6 个证据来源 / 多维度交叉验证）、推理逻辑性 88 分（因果链清晰 / 考虑了替代假设）、安规合规性 85 分（引用了具体条款 / 标注了安全注意事项）、可操作性 82 分（步骤具体可执行 / 但缺少预估耗时）、历史一致性 90 分（与历史案例 TK-202607001 结论一致）。加权总分 = 90×0.25 + 88×0.25 + 85×0.20 + 82×0.20 + 90×0.10 = 87 分，等级 B（良好）。

**第九步：闭环决策**

87 分 ≥ 70 分 → 直接进入 SafetyReview，不触发重规划。

**第十步：SafetyReview + FinalResponse + MemorySave**

RiskReviewAgent 安全审查：is_compliant = true，risk_level = HIGH（建议人工确认），violations = []，suggestions = ["逆变器柜门打开后通风 5 分钟再操作，高温表面防烫伤"]。FinalResponse 组装最终输出，包含完整诊断报告 + Judge 评分 + 安全审查结论。MemorySave 持久化会话记录。前端收到 SSE 流式推送的内容和结构化诊断数据后，渲染诊断报告卡片。

**第十一步：ActiveLearning（后续）**

用户阅读诊断报告，通过前端反馈按钮提交评价"准确"。系统触发学习机制：诊断文本向量化存入 ChromaDB 长期记忆集合。系统检查 long_term_memory 中 "逆变器 + 通讯中断 + IGBT 过热" 模式的出现次数（第 2 次，未到 3 次阈值）。检查累积新案例数量（第 28 个，未到 50 个阈值）。学习完成，等待下一次触发。

---

## 十、部署架构

### 10.1 Docker Compose 服务拓扑

系统由 4 个 Docker 容器组成，通过 docker-compose.yml 定义和管理：

                        用户浏览器
                             │
                             │ HTTP (端口 80)
                             ▼
             ┌───────────────────────────────┐
             │  Nginx 容器 (frontend)         │
             │  • Vue 3 SPA 静态资源托管       │
             │  • /api/* 路径反向代理到 api    │
             │  • 端口映射: 80:80             │
             └───────────────┬───────────────┘
                             │ proxy_pass /api/* → api:8080
             ┌───────────────▼───────────────┐
             │  FastAPI 容器 (api)            │
             │  • 主应用服务                   │
             │  • DeepSeek API 调用            │
             │  • LangGraph 编排引擎           │
             │  • 端口映射: 8080:8080          │
             │  • 健康检查: /health            │
             └───────┬───────────┬───────────┘
                     │           │
     ┌───────────────▼──┐  ┌─────▼──────────────┐
     │  ChromaDB 容器    │  │  MCP Server 容器    │
     │  • 向量数据库      │  │  • 工具服务器        │
     │  • 持久化存储      │  │  • 数据文件挂载      │
     │  • 端口: 8000     │  │  • 端口: 9901       │
     │  • IS_PERSISTENT  │  │  • 访问权限只读      │
     └──────────────────┘  └────────────────────┘

### 10.2 数据持久化

| 数据 | 存储方式 | 挂载点 | 备份策略 |
|------|---------|--------|---------|
| 向量嵌入 | ChromaDB 内置 SQLite | `./knowledge_db/chroma.sqlite3` | 每日自动备份到 ./backup/ 目录 |
| 用户上传文档 | 本地文件系统 | `./data/raw/` | 上传时同步复制到 MinIO（可选） |
| 诊断历史 | 内存 + JSON 文件 | `./data/processed/diagnosis_history.json` | 每日滚动归档 |
| 环境配置 | .env 文件 | `./.env` | 纳入版本管理（去除 API Key） |

### 10.3 启动方式

| 方式 | 命令 | 适用场景 |
|------|------|---------|
| **本地开发** | 激活虚拟环境后执行：uvicorn app.main:app --reload --host 0.0.0.0 --port 8080 | 代码开发和调试，支持热重载 |
| **Docker 部署** | docker-compose up -d | 生产环境，一键启动 4 个服务 |
| **快速脚本** | bash start.sh | 开发/测试环境快速启动 |

### 10.4 资源配置建议

| 部署模式 | CPU | 内存 | 磁盘 | GPU | 网络要求 | 适用场景 |
|----------|-----|------|------|-----|---------|---------|
| **纯离线** | 2 核 | 4 GB | 10 GB SSD | 不需要 | 不需联网 | 涉密场站，完全不连接互联网 |
| **标准离线** | 4 核 | 8 GB | 15 GB SSD | 推荐（显存 ≥ 8GB） | 不需联网 | 有本地 GPU 的场站，运行 Ollama |
| **生产在线** | 4 核 | 8 GB | 50 GB+ SSD | 不必须 | 需访问 DeepSeek API | 全场站使用，性能最优模式 |
| **大规模集群** | 8 核 | 16 GB | 100 GB+ NVMe | 推荐 | 需访问 DeepSeek API | 多场站集中部署，高并发场景 |

---

## 十一、产品核心优势总结

| 序号 | 核心优势 | 技术实现 | 用户价值 |
|------|---------|---------|---------|
| ① | **全链条闭环** | 8 个诊断节点 + 置信度阈值 0.7 的闭环重规划机制（最多 2 次重试），Diagnosis → Judge 评估 → SafetyReview → ActionRecommend → 反馈 → 学习 | 诊断质量有兜底保障，不会输出不可信的结果；低分自动重试，高分直接通过 |
| ② | **自我进化能力** | 主动学习系统：成功案例自动入库向量化 → 同模式 ≥ 3 次自动提炼 Skill → 累积 ≥ 50 案例触发 LoRA 增量微调 | 系统越用越聪明，越用越懂本场站的设备特性；知识从被动变主动 |
| ③ | **LangGraph 多智能体编排** | Supervisor Pattern + 7 个专业 Agent + 35 个状态键 + 12 个生命周期 Hook，状态驱动、可视化、可中断 | 避免单一 Agent 的"幻觉"问题，多 Agent 交叉验证确保质量；可随时插入人工审批 |
| ④ | **轻量私有化部署** | Docker Compose 一键部署，ChromaDB 嵌入式向量数据库（零运维），三种部署模式（纯离线/离线+Ollama/生产在线） | 数据 100% 留在场站内部，满足涉密要求；无需专业 AI 运维人员 |
| ⑤ | **RAG 与知识图谱双引擎** | 向量检索（BGE-Large-ZH, 1024维）+ BM25 关键词 + 知识图谱（48 种设备 × 100+ 故障模式）+ RRF 融合 + BGE Reranker 精排 | 诊断准确率提升 6.56%+（知识图谱贡献）；三重检索互补，精确和语义两者兼得 |
| ⑥ | **标准 MCP 工具协议** | 基于 Anthropic MCP 标准的工具服务器，工具注册/发现/调用统一接口，6 个内置工具 + 无限第三方扩展 | 工具生态可扩展，未来可接入天气预报/地理信息/ERP 等第三方 MCP 工具 |
| ⑦ | **SCADA 实时数据接入** | Modbus TCP / IEC 61850 / OPC UA 三大工业协议适配器 + 环形缓冲区 + 故障窗口自动提取（±5 分钟） | 告警触发即自动诊断，无需人工查数据、截图、打字；数据完整度和精度远超人工 |
| ⑧ | **多模态融合诊断** | 文本（DeepSeek V4 Pro）+ 图像（Qwen-VL-Max）+ 声音（AST）+ Cross-Attention 融合 + 联合推理 | 拍照上传即可自动分析热图和外观异常；图像客观数据消除人工描述的主观偏差 |
| ⑨ | **Judge 五维度质量评估** | 证据充分性 + 推理逻辑性 + 安规合规性 + 可操作性 + 历史一致性，五个维度加权打分，0-100 分量化可信度 | 用户清清楚楚知道"这个诊断有多大把握"，而非盲猜 AI 是否准确 |
| ⑩ | **三层记忆系统** | 短期记忆（会话级, OrderedDict）+ 工作记忆（任务级, StateGraph 状态键）+ 长期记忆（永久级, ChromaDB + 时间衰减） | 记住历史、理解上下文、越用越聪明，跨会话知识复用 |

---

## 十二、一句话总结

**驭能**是一个基于 **LangGraph 多智能体编排**（Supervisor Pattern + 7 个专业 Agent + 35 个状态键 + 12 个生命周期 Hook）构建的新能源场站智能急诊系统，具备**全链条闭环**（8 诊断节点 + 置信度 0.7 闭环重规划）、**自我进化能力**（主动学习 + Skill 自动生成 + LoRA 增量微调）、**SCADA 实时数据接入**（Modbus TCP / IEC 61850 / OPC UA 三大工业协议）、**多模态融合诊断**（文本 + 图像 + 声音 Cross-Attention 融合）、**Judge 五维度质量评估**（证据/逻辑/安规/操作/历史，0-100 分量化）、**轻量私有化部署**（Docker 一键启动，数据 100% 不出场站）六大核心能力——它不做"大而全"的平台，而是专注做好"停机急救"这一件事，把它做到极致。

---

> **文档维护说明**：本文档基于 驭能 v3.0 功能规划编写，对应代码仓库 `/home/an/项目/驭能/`。文档中的架构描述、配置参数和性能指标均为设计目标，实现过程中需根据实际环境测试验证和同步更新。
