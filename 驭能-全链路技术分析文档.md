# 驭能-风电运维 — 全链路技术分析文档

> **项目**: 新能源场站非计划停机智能诊断系统
> **版本**: v2.1
> **更新日期**: 2026-08-10
> **仓库**: `/home/an/项目/驭能/`

---

## 一、项目需求分析

### 1.1 产品定位

面向宁夏新能源场站（风电场 + 光伏电站）的**AI 智能诊断系统**。当风机、光伏逆变器、变压器等核心设备突发故障时，系统在 3-5 分钟内自动完成 **故障诊断 → 处置方案 → 安规审查 → 决策指导** 的全链路闭环。

### 1.2 核心业务需求

| 需求编号 | 需求描述 | 优先级 |
|----------|---------|--------|
| REQ-01 | 接收设备告警后，自动抓取 SCADA 故障前后时序数据 | P0 |
| REQ-02 | 基于故障描述，DeepSeek V4 Pro 进行 Chain-of-Thought 诊断推理 | P0 |
| REQ-03 | 输出 9 项结构化诊断报告（告警摘要/初步判断/分析依据/可能原因/排查步骤/处理建议/安全风险/派单建议/风险自复核） | P0 |
| REQ-04 | 诊断后自动审查安全合规性，引用电力安规条款 | P0 |
| REQ-05 | 支持图像分析（红外热像/电气图/设备外观）和音频分析（轴承摩擦/齿轮断齿/电弧放电） | P1 |
| REQ-06 | 支持用户反馈（准确/部分准确/不准确）驱动学习进化 | P1 |
| REQ-07 | 知识库支持文档上传（PDF/DOCX/XLSX/MD）自动解析入库 | P1 |
| REQ-08 | 支持 Modbus TCP / IEC 61850 / OPC UA 三种工业协议对接 | P1 |
| REQ-09 | 离线降级：网络断开时自动切换本地规则引擎/本地 GGUF 模型 | P1 |
| REQ-10 | 多模型并行投票诊断（DeepSeek Chat + Reasoner + Qwen-Max） | P2 |
| REQ-11 | GraphRAG 知识图谱（48 种设备 × 100+ 故障模式图关联） | P2 |
| REQ-12 | 失败案例自动分析 + Skill 自动生成 | P2 |
| REQ-13 | RLHF 反馈学习 + Benchmark 基准测试 | P3 |
| REQ-14 | Dashboard 项目进度监控 | P3 |

### 1.3 非功能需求

| 需求编号 | 需求描述 | 指标 |
|----------|---------|------|
| NFR-01 | 诊断响应时间 | 单次诊断 < 60 秒 |
| NFR-02 | 并发能力 | 支持 50 并发无死锁 |
| NFR-03 | 高可用 | API 不可用时自动降级为规则引擎 |
| NFR-04 | 可扩展性 | 子智能体插件式注册，热插拔 |
| NFR-05 | 安全性 | JWT 认证 + 限流 60/min/IP + 请求审计日志 |
| NFR-06 | 数据持久化 | SqliteSaver 持久化诊断状态，支持回放 |
| NFR-07 | SSE 流式输出 | 诊断过程逐字流式推送 |
| NFR-08 | 知识库检索速度 | 单次检索 < 100ms |
| NFR-09 | 知识库加载速度 | 500 条文档 < 30 秒 |

---

## 二、系统架构总览

```
┌─────────────────────────────────────────────────────────────────┐
│                        前端展示层                                │
│  Vue 3 + Element Plus + ECharts + Three.js (3D 风电场)          │
│  Vite 构建 | Pinia 状态管理 | Vue Router | Axios                │
└───────────────────────────┬─────────────────────────────────────┘
                            │ HTTP/SSE/WebSocket
┌───────────────────────────▼─────────────────────────────────────┐
│                      FastAPI 网关层                              │
│  Uvicorn ASGI | CORS | JWT 认证 | 限流 60/min | 请求审计         │
│  统一响应: {code, data, message} | 统一异常: HTTP/422/500       │
├─────────────────────────────────────────────────────────────────┤
│                 LangGraph 多智能体编排引擎                        │
│  StateGraph → Supervisor 路由 → 6 子智能体并行/条件执行          │
│  SqliteSaver 持久化 | 重规划循环 ≤ 2 次 | SSE 节点级推送         │
├─────────────────────────────────────────────────────────────────┤
│  诊断子智能体  │  知识问答子智能体  │  SCADA子智能体  │  聊天     │
│  (9节点)       │  (6节点)          │  (5节点)       │  (2节点)  │
├─────────────────────────────────────────────────────────────────┤
│  多模态子智能体 │  报告子智能体      │  质量裁判子智能体           │
│  (4节点)       │  (3节点)          │  (五维度评分)              │
├─────────────────────────────────────────────────────────────────┤
│              RAG 检索引擎 (混合检索 + 重排 + 知识图谱)            │
│              SCADA 数据层 (3 协议适配 + 环形缓冲区)              │
│           三层记忆系统 (短期/工作/长期 + 时间衰减)               │
│              多模态分析 (Qwen-VL + librosa 音频)                 │
│        主动学习 (案例自动入库 + Skill 自动生成)                   │
└─────────────────────────────────────────────────────────────────┘
```

---

## 三、各模块技术栈与算法详解

### 3.1 Web 框架层 (app/main.py)

**技术栈**:
- **FastAPI** (0.115+) — 异步 Web 框架，原生 OpenAPI/Swagger
- **Uvicorn** — ASGI 服务器，事件循环驱动
- **Starlette** — 中间件系统（CORS / 统一响应包装 / 异常处理）

**关键设计模式**:
- **统一响应中间件**: 所有 JSON 响应自动包装为 `{code: 0, data: ..., message: "success"}`，流式/SSE/静态资源透传不包装
- **统一异常处理**: StarletteHTTPException(404/422) → Pydantic RequestValidationError → 兜底 Exception(500)
- **Lifespan**: 启动时注册 6 个子智能体 + 异步预热嵌入模型和重排序模型
- **中间件链**: CORS → 限流 → JWT 认证 → 请求审计 → 统一响应

### 3.2 LLM Provider 层 (app/agent/llm_provider.py)

**技术栈**:
- **OpenAI Python SDK** — 兼容 DeepSeek API / DashScope API
- **llama-cpp-python** — GGUF 本地 CPU 推理
- **Threading** — 后台恢复探测线程

**核心算法**: **三层混合部署自动降级**

```
┌─────────────────────────────────────┐
│ L1 (主):  DeepSeek V4 Pro API       │ ← 在线，最快最准
│   超时/401/500 自动降级               │
├─────────────────────────────────────┤
│ L2 (降1): Qwen GGUF 本地 CPU 推理    │ ← 离线 LLM
│   GGUF 加载失败自动降级              │
├─────────────────────────────────────┤
│ L3 (降2): 规则引擎 (24 种故障规则)    │ ← 纯离线，无 LLM
└─────────────────────────────────────┘
```

**恢复策略**: 后台线程每 60 秒探测上层 Provider，可用时自动回升。

**规则引擎匹配算法**: 关键词全匹配搜索 (OFFLINE_RULES 字典，24 种故障模式)，匹配不到返回通用故障处置模板。

**模型配置**:
| 模型 | 用途 | Provider |
|------|------|----------|
| deepseek-chat (V4 Pro) | 主推理 | DeepSeek API |
| deepseek-reasoner (R1) | 深度推理 | DeepSeek API |
| qwen-max | 中文旗舰 | 阿里 DashScope |
| Qwen GGUF (本地) | 离线推理 | llama-cpp CPU |
| 规则引擎 | 纯离线兜底 | Python dict |

### 3.3 LangGraph 多智能体编排层 (app/graph/)

**技术栈**:
- **LangGraph** (0.2+) — StateGraph 状态图工作流引擎
- **langgraph-checkpoint-sqlite** — SqliteSaver 状态持久化
- **MemorySaver** — 降级内存模式

**状态定义** (app/graph/state.py):

**AgentState TypedDict** — 38 个状态字段，分为 11 个类别：

| 类别 | 字段数 | 说明 |
|------|--------|------|
| 输入层 | 3 | input / cleaned_input / rewritten_query |
| 会话标识 | 4 | session_id / task_id / user_id / trace_id |
| 意图与实体 | 3 | intent / confidence / entities |
| 计划与执行 | 4 | plan_steps / step_results(Append) / execution_result / current_step_index |
| 证据链 | 4 | evidence / evidence_score / evidence_coverage / evidence_warnings |
| 诊断核心 | 3 | diagnosis_result / risk_level / next_action |
| 循环控制 | 3 | loop_count / review_decision / max_retries |
| RAG/工具 | 2 | rag_results / tool_result |
| 记忆层 | 4 | memory_context / skill_context / history / matched_skill |
| SCADA | 3 | scada_data / scada_timestamp / device_id |
| 安全验证 | 4 | validation_warnings(Append) / permission_granted / alarm_level / approval_required |
| 最终输出 | 1 | final_response |

**Graph 编排引擎** (app/graph/builder.py):

完整工作流:
```
START → precheck → context_load → router
  ├─ knowledge_qa → quality_gate → memory_save → END
  ├─ diagnosis_parallel (SCADA + Multimodal 并行) → diagnosis → {重规划循环 ≤2次} → quality_gate → memory_save → END
  └─ chat → quality_gate → memory_save → END
```

**核心算法**:

1. **意图路由** (route_by_intent): 基于 intent 字段分类 → knowledge_qa / diagnosis_parallel / chat 三条支路

2. **重规划循环** (route_after_diagnosis): 置信度 < 0.7 且循环次数 ≤ max_retries(2) 时重新执行 diagnosis 节点

3. **质量门控** (_quality_gate_wrapper): 置信度 < 0.7 时调用 Judge Agent 五维度评分，取 `max(confidence, judge_score/100)`

4. **并行调度** (diagnosis_parallel_node): 使用 `asyncio.gather` 并行执行 SCADA + Multimodal 子智能体

5. **状态持久化**: AsyncSqliteSaver (aiosqlite) 持久化完整状态，支持 `/api/trace/{id}/replay` 回放

**子智能体基类** (app/graph/sub_agent.py):
- 每个 Skill 编译为独立 LangGraph CompiledGraph（子图）
- 统一接口: `build()` 构建子图，`arun(state)` 异步执行
- 插件式注册: `SubAgentRegistry.register(SubAgent)`，支持并行调度

### 3.4 Agent 层 (app/agent/)

#### 3.4.1 RouterAgent — 意图路由器
- **算法**: LLM 9 分类意图识别 (Few-shot Prompt)
- **分类**: FAULT_DIAGNOSIS / ALARM_DIAGNOSIS / SAFETY_QA / DEVICE_STATUS / DEVICE_PROFILE / ALARM_QUERY / LOG_ANALYSIS / TICKET_QUERY / GENERAL_CHAT
- **输出**: JSON `{intent, confidence, entities, reason}`

#### 3.4.2 DiagnosisAgent — 核心诊断引擎
- **算法**: **Chain-of-Thought (CoT) 四步推理** + 3 个 Few-shot 示例
  1. 关键信息提取 (设备类型/异常参数/告警级别/持续时间)
  2. 症状-故障匹配 (关键词与知识库已知故障模式匹配)
  3. 根因推理 (触发条件满足度 + 参数变化吻合度 + 历史案例支持度)
  4. 最终结论 (综合证据 → 最可能根因 + 置信度)
- **多源上下文注入**: 知识图谱上下文 → Skill Prompt → 气象数据 → SCADA 数据
- **输出**: 9 项结构化诊断报告 + JSON 摘要 `{root_cause, confidence, risk_level, evidence_sufficient, recommend_dispatch, urgency}`
- **Ensemble 模式**: 自动尝试 deepseek-reasoner 深度推理

#### 3.4.3 MultiModelClient — 多模型并行引擎
- **算法**: **多模型投票融合 (Ensemble)**
  - `deepseek-chat` (V4 Pro, 主模型)
  - `deepseek-reasoner` (R1, 深度推理)
  - `qwen-max` (阿里旗舰)
- **融合算法**:
  1. 并行调用多个模型 (ThreadPoolExecutor)
  2. 取置信度均值: `avg_confidence = mean(scores)`
  3. 一致性加分: 2 个以上模型差异 < 0.3 则 `ensemble_confidence = min(avg + 0.10, 0.95)`
  4. 风险取最严重: `max(risk_levels, key=severity_order)`

#### 3.4.4 JudgeAgent — 五维度质量评估
- **算法**: **加权多维度评分 + 等级映射**
  - 证据充分性 (25%) — 来源数量 + 交叉验证
  - 推理逻辑性 (25%) — 因果链完整度 + 替代假设
  - 安规合规性 (20%) — 条款引用 + 高风险标注
  - 可操作性 (20%) — 步骤具体性 + 工具清单
  - 历史一致性 (10%) — 故障模式吻合度
- **等级映射**: A(90-100) / B(80-89) / C(70-79) / D(60-69) / F(<60)
- **重规划触发**: < 70 分触发重规划，< 60 分两次仍不合格则降级人工

#### 3.4.5 RiskReviewAgent — 安全审查
- **算法**: LLM 安规合规审查（安全合规性 → 操作风险评估 → 缓解措施 → 最终建议 JSON）

#### 3.4.6 SubagentExecutor — 4 子 Agent 并行执行器
- **算法**: asyncio.gather 并行调用 regulation / metrics / log / ticket 4 个子 Agent
- 输出汇总为 `SubagentTask` 列表

#### 3.4.7 CaseReasoner — 离线案例推理 (app/agent/case_reasoner.py)
- **算法**: 从反馈数据中提取成功案例，BGE-Large-ZH 编码后 ChromaDB 检索历史相似案例

### 3.5 RAG 检索引擎层 (app/rag/)

#### 3.5.1 混合检索引擎 (hybrid_search.py)

**算法**: **三重检索 + RRF 融合 + BGE Reranker 精排**

```
向量检索 (BGE-Large-ZH 1024D, ChromaDB HNSW) ─┐
BM25 关键词检索 (电力领域词典 55 词)           ├─→ RRF 融合 (Top-20) → BGE Reranker → Top-5
知识图谱上下文扩展                               ┘
```

**RRF (Reciprocal Rank Fusion) 算法**:
```
score(d) = Σ weight_s / (k + rank_s(d))
```
- `k = 60` (平滑常数，防止 rank=1 权重过大)
- 权重: 向量=1.0, BM25=1.0, 图谱=0.8
- 取 Top-20 候选进入精排

**BM25 关键词检索**:
- TF-IDF 加权，对电力领域词典 55 个专业词做双字+专业词混合分词
- Cache 优化: LRU(128, TTL=30s)，相同查询 < 100ms 命中的直接返回

**FastKnowledgeStore**:
- 188 条预置电力知识，KNOWLEDGE_FILE (JSON) 持久化
- 检索时生成 sha256 查询键做缓存

#### 3.5.2 BGE Reranker 精排 (rerank.py)

**技术栈**: **BAAI/bge-reranker-v2-m3** (CrossEncoder 交叉编码)

**算法**: **Cross-Encoder 深层语义交互精排**
- 将 (query, document) 拼接后完整送入 Transformer，计算语义相关性分数
- 比双塔模型 (Bi-Encoder) 精度更高但速度较慢
- 降级策略三级: BGE CrossEncoder → DeepSeek API 重排 → 关键词重叠度计算

**关键词重叠度算法 (降级方案)**:
```
score = overlap * 0.5 + length_bonus(0.1 if 100<len<2000) + rrf_score * 0.1
overlap = |q_tokens ∩ d_tokens| / |q_tokens|
```

#### 3.5.3 知识图谱 (knowledge_graph.py)

**数据量**: 48 种设备类型 × 100+ 故障模式

**覆盖类别**:
1. 风力发电设备 (8 种): 风电机组/齿轮箱/发电机/偏航系统/变桨系统/叶片/主轴承/变频器
2. 光伏发电设备 (5 种): 逆变器/光伏组件/汇流箱/直流柜/跟踪系统
3. 储能设备 (3 种): 储能电池/BMS/PCS
4. 变电一次设备 (7 种): 变压器/断路器/隔离开关/CT/PT/避雷器/母线
5. 无功补偿设备 (3 种): SVG/电容器/电抗器
6. 线路设备 (4 种): 架空线路/电缆/杆塔/绝缘子
7. 保护测控设备 (5 种): 保护装置/测控装置/故障录波器/PMU/安全稳定装置
8. 站用电设备 (5 种): 直流屏/UPS/蓄电池/站用变/柴油发电机
9. 系统控制设备 (4 种): AGC/AVC/EMS/SCADA
10. 通讯设备 (4 种): 交换机/路由器/光端机/规约转换器

**每设备结构**: `{alarms, causes, steps, safety}`

**实体提取算法**: 正则匹配关键词 + 设备类型映射
- `extract_entities()`: 从故障文本中提取 device_type / device_id / fault_keywords
- `build_graph_context()`: 构建带格式的知识图谱上下文文本

#### 3.5.4 GraphRAG 引擎 (graphrag.py)

**技术栈**: Neo4j (图数据库) → 降级 NetworkX (内存图)

**算法**: **BFS 层级扩展** (降级模式)
- 从故障设备节点出发，沿 `HAS_COMPONENT` → `HAS_FAULT` → `CAUSED_BY` → `RESOLVED_BY` 路径扩展
- 生成结构化图上下文供 RAG 使用

**图结构**:
```
设备类型 → 子类型 → 组件 → 故障模式 → 原因 → 解决方案
```

#### 3.5.5 文档解析器 (document_parser.py)

**技术栈**:
| 格式 | 库 | 增强功能 |
|------|---|---------|
| PDF | PyMuPDF (fitz) | 表格提取 + 字体层级(H1>18pt, H2>15pt, H3>13pt) + OCR(PaddleOCR可选) |
| DOCX | python-docx | 段落样式 Heading 1/2/3 + 表格 XML 提取 |
| EXCEL | openpyxl | 多 Sheet + 表头检测(第一行) |
| MD/TXT | 正则 | ### 章节层级 + Markdown 结构 |

**处理流程**: 文件路径 → 自动识别格式 → 分块解析 → 清洗 → 向量化入库

#### 3.5.6 向量存储 (vector_store.py)

**技术栈**: **ChromaDB** (HNSW 近似最近邻) + **BCE-Large-ZH** 嵌入

**算法**: **HNSW (Hierarchical Navigable Small World)** 向量索引
- 本地: maidalun1020/bce-embedding-base_v1 (768D, SentenceTransformer)
- API: DeepSeek API 嵌入 (OpenAI embeddings endpoint)
- 降级: 纯 BM25 关键词检索
- **线程安全**: threading.Lock 双重检查锁定 (double-checked locking)

#### 3.5.7 API Embedding 重排 (api_embed.py)

**技术栈**: DeepSeek Chat-based 重排序

**算法**: **逐文档 LLM 相关性评分**
- 对每个文档: `"评估以下文档与查询的相关性，仅输出0到1之间的分数"`
- 取前 5 个最高分文档

### 3.6 SCADA 数据层 (app/scada/)

**技术栈**: 三种工业协议 + 环形缓冲区 + 数据标准化

#### 3.6.1 协议适配器 (protocols/)

| 协议 | 库 | 适用设备 |
|------|---|---------|
| **Modbus TCP** | pymodbus | 逆变器/汇流箱/环境监测仪 |
| **IEC 61850** | iec61850 (libiec61850) | 风机主控/保护装置/测控装置 |
| **OPC UA** | opcua-asyncio | 变压器/箱变/变电站后台 |

**统一接口** (ProtocolAdapter 基类):
- `connect()` / `disconnect()` / `read_all()` / `read_point()`
- 两种模式: mock (模拟数据) / real (真实硬件)

**协议工厂** (protocol_factory.py): 根据 DeviceConfig 自动创建对应适配器

#### 3.6.2 环形缓冲区 (ring_buffer.py)

**算法**: **线程安全的定长循环队列 (Thread-safe Ring Buffer)**
- 容量: 1,800,000 条 (1000 测点 × 1 次/秒 × 60 秒 × 30 分钟)
- 数据结构: `collections.deque(maxlen=capacity)` + `threading.Lock`
- 核心操作:
  - `push()`: 单点写入（自动覆盖最旧数据）
  - `push_batch()`: 批量写入
  - `get_window(center_time, ±min)` → 故障时间窗口提取
  - `get_recent(seconds)` → 最近 N 秒数据
  - `query(device_id, point_name, time_range)` → 灵活多维查询

#### 3.6.3 数据标准化器 (data_normalizer.py)

**算法**:
- **标准化**: 三种协议输出 → 统一 `ScadaDataPoint` 格式
- **统计分析**: min / max / mean / std (样本标准差: `n-1` 贝塞尔校正)
- **异常检测**: 基于 Normal Ranges 阈值判断（如逆变器温度 20-75°C, 振动 0-3mm/s 等 3 设备 × 多参数的范围检查）

#### 3.6.4 故障窗口提取器 (window_extractor.py)
- **算法**: 以告警时刻为中心，提取前后 ±5 分钟 SCADA 数据窗口

### 3.7 多模态分析层 (app/multimodal/)

#### 3.7.1 图像分析器 (image_analyzer.py)

**技术栈**: **Qwen-VL-Max** (阿里 DashScope 多模态大模型)

**分析模式**:
1. **红外热像分析** (thermal): 温度异常区域检测（温差 > 5°C 默认异常），重点关注 IGBT 模块/直流母线
2. **电气图分析** (electrical): 识别一次接线图/二次回路图/保护逻辑图
3. **设备外观分析** (appearance): 裂纹/变形/腐蚀/漏油/散热通道堵塞检测
4. **自动分析** (auto): 先判断图像类型，再针对性分析

**降级策略**: API 不可用时返回 Base64 图片元数据

#### 3.7.2 音频分析器 (audio_analyzer.py)

**技术栈**: **librosa** (音频信号处理) + **numpy** (FFT 频谱分析)

**算法**: **频域特征提取 + 故障模式匹配**

**提取特征**:
- RMS 能量 (均值/最大值/标准差)
- 频谱质心 (Spectral Centroid)
- 零交叉率 (Zero-Crossing Rate)
- 频谱平坦度 (Spectral Flatness)
- 四频段能量分布: 0-500Hz / 500-2kHz / 2-5kHz / 5-10kHz

**故障声音模式库** (6 种):
| 模式 | 频段 | 特征 |
|------|------|------|
| 轴承干摩擦 | 4-10kHz | 持续高频嘶嘶声 |
| 齿轮断齿 | 啮合频率 | 周期性撞击声 |
| 电弧放电 | 全频段 | 不规则噼啪声 |
| 变压器异常嗡声 | 50Hz 基频 | 100Hz 谐波增大 |
| 叶片不平衡 | 1P 转频 | 1倍转频振动超标 |
| 对中不良 | 1P/2P | 2倍转频突出 |

**匹配算法**: 加权评分
```
score = RMS_std(>0.01 → 0.15) + centroid(>3000 → 0.2) + flatness(<0.3 → 0.15) + device_type_match(0.25)
```

**降级策略**: librosa 不可用 → soundfile 基础信息 → 纯元数据模式

#### 3.7.3 Cross-Attention 融合 (fusion.py)

**技术栈**: **SentenceTransformer** × 3 (BCE-Large-ZH / CLIP-ViT-B-32 / MiniLM-L6) + **NumPy** 矩阵运算

**算法**: **以文本为 Query 的 Cross-Attention 多模态融合**

**架构**:
```
1. 分模态编码: 文本(BCE→2048D) + 图像(CLIP→512D→2048D投影) + 音频(MiniLM→384D→2048D投影)
2. Cross-Attention: Text(Query) × Image(Key/Value) + Text(Query) × Audio(Key/Value)
3. 特征融合: Concat → Mean Pooling → L2 Norm → 2048D 融合向量
4. 一致性判断:
   - |max_score - min_score| < 0.2 → consistent (信心+0.05)
   - |max_score - min_score| > 0.5 → contradiction (信心-0.05)
   - 否则 → partial (信心+0.02)
```

**核心公式** (缩放点积注意力):
```
Attention(Q, K, V) = softmax(Q·K^T / √d_k) · V
```

**线性投影层**: 正交初始化权重 `W = randn × 0.02`，`projected = X @ W + b`

### 3.8 记忆系统 (app/memory/)

**技术栈**: **OrderedDict** (短期/工作记忆) + **ChromaDB** (长期记忆)

**三层记忆模型**:

| 层级 | 存储 | 生命周期 | 容量 |
|------|------|---------|------|
| 短期记忆 | OrderedDict 内存 | 会话级，最近 3 轮 | 3 轮对话 |
| 工作记忆 | OrderedDict 内存 | 任务级，35 状态键 | 1 个任务 |
| 长期记忆 | ChromaDB | 永久，时间衰减 | 无上限 |

**时间衰减算法** (长期记忆检索):
```
weight = base_weight * 0.5^(days_since_creation / 180)
```
- 半衰期: 180 天（半年）
- 检索时按 `decay_weight * relevance_score` 排序

### 3.9 主动学习层 (app/learning/)

#### 3.9.1 案例自动入库 (case_ingestion.py)
- **触发**: 用户反馈"准确" + 置信度 ≥ 0.7
- **流程**: 提取诊断文本 → BGE-Large-ZH 编码 → 存入 ChromaDB `diagnosis_cases` 集合
- **元数据**: device_type / fault_type / timestamp / confidence / feedback_label

#### 3.9.2 Skill 自动生成 (skill_generator.py)
- **触发**: 相同故障模式长期记忆中累积 ≥ 3 次
- **算法**: LLM 分析 3 个相似案例 → 提取共性特征 → 生成标准化 Skill (Prompt 模板 + 诊断流程 + 关键检查点)
- **产出**: 自动注册到 SkillRegistry

### 3.10 MCP 工具层 (mcp_server/)

**技术栈**: **Python MCP SDK** (Model Context Protocol)

**6 个 MCP 工具**:
1. `get_device_status` — 设备实时状态查询
2. `get_alarm_history` — 告警历史查询
3. `get_device_logs` — 运行日志查询
4. `get_defect_tickets` — 缺陷工单查询
5. `search_safety_rules` — 电力安规检索
6. `get_device_profile` — 设备台账查询

**工具注册系统** (app/tools/registry.py):
- 语义检索: `search_by_intent(intent)` 根据诊断意图匹配工具
- 关键词搜索: `search(keyword)` 在工具名/描述/标签中匹配
- 风险等级标注: LOW / MEDIUM / HIGH

### 3.11 服务层 (app/services/)

| 模块 | 技术 | 功能 |
|------|------|------|
| field_services.py | 模拟数据 | 气象上下文/安全清单/检修窗口生成 |
| data_validator.py | Pydantic | 输入数据校验 |
| alert_filter.py | 规则匹配 | 告警去重与过滤 |
| scheduler.py | asyncio | 定时任务调度 |

### 3.12 工具层 (app/utils/)

| 模块 | 技术 | 功能 |
|------|------|------|
| cache.py (LRUCache) | OrderedDict + threading.Lock | 线程安全 LRU + TTL 缓存 |
| circuit_breaker.py | 状态机 (Closed→Open→Half-Open) | 防止级联故障熔断器 |
| access_log.py | FileHandler | 请求审计日志 |
| auth.py | JWT (python-jose) | 用户认证 |
| errors.py | 自定义异常 | 统一错误码 |

### 3.13 前端技术栈 (frontend/)

| 层级 | 技术 |
|------|------|
| 框架 | **Vue 3** (Composition API + `<script setup>`) |
| UI 库 | **Element Plus** 2.14 |
| 构建工具 | **Vite** 6 |
| 状态管理 | **Pinia** 4 + persistedstate 插件 |
| 路由 | **Vue Router** 5 |
| HTTP | **Axios** 1.19 |
| 图表 | **ECharts** 6 + vue-echarts 8 |
| 流程图 | **Vue Flow** 1.48 (知识图谱可视化) |
| 3D 渲染 | **Three.js** (3D 海上风电场背景) |
| 类型检查 | **TypeScript** + vue-tsc |

---

## 四、数据流全链路

```
用户输入故障描述
    │
    ▼
[意图路由] RouterAgent → LLM 9分类
    │
    ├─ FAULT_DIAGNOSIS / ALARM_DIAGNOSIS
    │   ├─ [并行] SCADA子智能体: 抓取故障窗口时序数据
    │   ├─ [并行] 多模态子智能体: 图像/音频分析 (如有)
    │   ├─ [并行] 预测监控子智能体: 趋势预测
    │   ▼
    │   [诊断] DiagnosisAgent → CoT 四步推理
    │   ├─ 知识图谱实体提取 + 上下文扩展
    │   ├─ RAG: 三重检索+RRF融合+BGE精排→Top5知识
    │   ├─ 多模型Ensemble: deepseek-chat+reasoner+qwen-max
    │   ├─ 气象数据 + 安全清单 + 检修窗口注入
    │   ▼
    │   [质量门控] 置信度<0.7 → JudgeAgent五维评分 → 重规划≤2次
    │   ▼
    │   [报告] Report子智能体 → 格式化报告
    │
    ├─ KNOWLEDGE_QA / SAFETY_QA
    │   └─ [知识问答] KnowledgeQA子智能体 → RAG检索+生成
    │
    └─ CHAT
        └─ [聊天] Chat子智能体 → LLM对话
    │
    ▼
[安全审查] RiskReviewAgent → 安规合规检查
    │
    ▼
[记忆存储] MemoryService → 短期+长期记忆持久化
    │
    ▼
[SSE 流式输出] → 用户界面
    │
    ▼
[RLHF 反馈] 准确→入库 / 部分准确→待审 / 不准确→负样本
    │
    ▼
[主动学习] 案例累积≥3 → Skill自动生成 → 注册到SkillRegistry
```

---

## 五、API 接口总览 (18 个路由模块)

| 模块 | 路由前缀 | 核心端点 |
|------|---------|---------|
| chat | `/api/chat` | POST 对话 / SSE 流式 |
| diagnosis | `/api/diagnosis` | POST 诊断 / POST 多模态诊断 / GET 诊断历史 |
| alarm | `/api/alarm` | POST 告警诊断 / GET 告警列表 |
| knowledge | `/api/knowledge` | POST 上传 / GET 搜索 / GET 图谱 / GET 健康 |
| feedback | `/api/feedback` | POST 提交反馈 / GET 统计 |
| trace | `/api/trace/{id}/replay` | GET 诊断回放 (LangGraph state_history) |
| scada | `/api/scada` | GET 设备列表 / POST 数据查询 / POST 告警窗口 |
| dashboard | `/api/dashboard` | GET 进度 / GET 阶段详情 |
| audit | `/api/audit` | GET 审计报告 |
| workorder | `/api/workorder` | POST 创建 / GET 列表 |
| benchmark | `/api/benchmark` | GET 测试用例 / POST 运行 |
| rlhf | `/api/rlhf` | GET 排行榜 / POST 对比提交 |
| auth | `/api/auth` | POST 登录 / GET 用户信息 |
| websocket | `/ws` | WebSocket 实时推送 |
| settings | `/api/settings` | GET/PUT 系统设置 |
| external | `/api/external` | 外部系统对接 |
| automation | `/api/automation` | 自动化规则管理 |
| field | `/api/field` | 现场运维工具 |
| tools | `/api/tools` | GET 工具列表 / GET 工具搜索 |
| skills | `/api/skills` | GET 技能列表 + 子智能体 |

---

## 六、测试体系

| 测试模块 | 用例数 | 覆盖范围 |
|----------|--------|---------|
| test_agents.py | 27 | Router / Judge / Diagnosis / RiskReview / SubAgent 执行器 |
| test_api.py | 42+ | 全部 18 个 API 模块端到端测试 |
| test_hooks.py | 16 | 12 个 Hook 生命周期触发 |
| test_tools.py | 22 | MCP 6 工具注册 / 执行 / 查询 |
| test_rag.py | 31+ | RAG 检索 / 重排 / 知识图谱 / 文档解析 |
| test_scada.py | 30+ | 协议适配 / 环形缓冲区 / 数据标准化 / 异常检测 |
| test_multimodal.py | 14 | 图像分析 / 音频分析 / Cross-Attention 融合 |
| test_mcp.py | 34 | 工具调用往返时间 / 异常场景 / 服务端处理器 |
| test_knowledge_init.py | 15 | 知识库加载时效 / 检索精度 / 图谱完备性 |
| test_performance.py | 9 | 50并发健康检查 / 30并发Dashboard / 缓冲区并发安全 |
| test_modules.py | 31 | 全模块导入验证 |
| **合计** | **327** | - |

---

## 七、部署架构

```
┌──────────────────────────────────────────┐
│ Docker Compose 生产环境                    │
├──────────────────────────────────────────┤
│  frontend (Nginx + Vue dist)  :80         │
│  backend  (Uvicorn FastAPI)   :8080       │
│  mcp      (MCP Server)         :9901      │
│  chroma   (ChromaDB 向量库)    :8000      │
│  (可选) neo4j (图数据库)       :7687      │
├──────────────────────────────────────────┤
│  数据卷:                                     │
│  - knowledge_db/ (ChromaDB + knowledge.json)│
│  - data/ (checkpoints.db / feedback / training)│
│  - logs/ (access.log)                        │
└──────────────────────────────────────────┘
```

---

## 八、总技术栈一览

| 层级 | 核心技术 | 版本 |
|------|---------|------|
| **后端框架** | FastAPI + Uvicorn | 0.115+ |
| **LLM 引擎** | DeepSeek V4 Pro (Chat) + R1 (Reasoner) + Qwen-Max | API |
| **本地 LLM** | Qwen GGUF + llama-cpp-python | CPU 推理 |
| **离线兜底** | 规则引擎 (24 种故障模式) | Python dict |
| **工作流** | LangGraph StateGraph + SqliteSaver | 0.2+ |
| **嵌入模型** | BAAI/bce-embedding-base_v1 (768D) | SentenceTransformer |
| **重排模型** | BAAI/bge-reranker-v2-m3 (CrossEncoder) | SentenceTransformer |
| **图像编码** | CLIP-ViT-B-32-multilingual-v1 (512D) | SentenceTransformer |
| **音频编码** | all-MiniLM-L6-v2 (384D) | SentenceTransformer |
| **向量数据库** | ChromaDB (HNSW 索引) | 0.5+ |
| **图数据库** | Neo4j → NetworkX (降级) | Python |
| **多模态视觉** | Qwen-VL-Max | DashScope API |
| **音频处理** | librosa + numpy FFT | Python |
| **文档解析** | PyMuPDF / python-docx / openpyxl | Python |
| **工业协议** | pymodbus / iec61850 / opcua-asyncio | Python |
| **MCP 协议** | Python MCP SDK | 1.5+ |
| **前端框架** | Vue 3 + Element Plus + ECharts | - |
| **3D 渲染** | Three.js (WebGL) | JS |
| **构建工具** | Vite 6 | JS |
| **容器化** | Docker + docker-compose | - |
| **缓存** | LRU Cache (OrderedDict + TTL) | Python |
| **熔断** | Circuit Breaker (状态机) | Python |
| **认证** | JWT (python-jose) | Python |
| **数据** | Pandas / NumPy / PyArrow | Python |
